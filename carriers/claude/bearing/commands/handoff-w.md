---
description: このセッションの baton を著し、machine-local な置き場へ書き出す —— handoff skill の `w` を、サジェストからそのまま選べる形にした別名
disable-model-invocation: true
---

`handoff` skill を、**引数 `w`（書く）** で実行すること。

⚠ **この command は別名にすぎず、手順を 1 文字も持たない。** 正本は skill が同梱する
`write.md` であり、⚠ **ここへ書き写せば正本が 2 つになる** —— **複製した側が先に古くなる。**

⚠ **これは書く側である ∴ 打てば今の baton が退避される。** 読むつもりなら
`/bearing:handoff-r` —— ⚠ **どちらの取り違えも取り消せない。**
