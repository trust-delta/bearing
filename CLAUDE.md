<!-- bearing:aim v0.19.0 dir=docs/aims sha=941df49f0cdaa3b2 -->
## aim frame

⚠ **この repo が aim corpus を持つなら、開発はそれによって駆動される。** `docs/aims/<slug>.md` の各ファイルが 1 つの aim（目的とその手段）であり、親子で目的を分解した木を成す。


- **aim に触れる前に `aim` skill を読むこと。** slug の付け方、body の section、木の保守、drift の検出と修復は、そこが唯一の正本である。
- **frontmatter は人間のもの、body はあなたのもの。** `aim:`（目的 1 文）・`parent:`（木の位置）・`state:`（open / done / dead）を書き換えてはならない。目的が動くべきだと考えたなら、候補を提案して人間に escalate する。確定は人間の act である。
- **目的を単独で決めない。** 手段は提案し、実装してよい。だが「何のためか」と「それが達成されたか」の宣言は人間が担う。あなたが body に記すマークは*実装された事実*であって、*目的が満たされたこと*ではない。
- **`[todo]` は、あなたが自力で完了を確認できることだけを書く。** 人間の観測・判断を完了条件に持つものを書いてはならない —— あなたに完結できないうえ、`open-todo`（＝ エージェントの残務）に人間待ちが混ざって数が嘘になる。人間の観測が要ると分かったら、todo を「**観測できるようにする**」へ書き換える。判断そのものが要るなら escalate する。
- **不可逆・破壊的な変更は、確信の度合いに関わらず、実行前に必ず人間へ出す。** 履歴の書き換え、force-push、データの喪失、ブランチの削除。
- **迷ったら escalate する。** 維持はあなたの仕事だが、判断を人間から奪わない。
<!-- /bearing:aim -->

## 記録の作法

⚠ **他人の系（ハーネス・API・外部の道具）の振る舞いを、日付の無い現在形で書いてはならない。** 観測した瞬間は真でも、向こうが変えた瞬間に黙って偽になり、**我々の機構はそれを検知しない** —— 踏むまで分からない。⚠ **対して、過去形の観測は決して偽にならない**（「2026-09-04 に測ったら X だった」は、X が変わっても真である）∴ **実測は「いつ・何を・どう観測したか」の形で書き、現在形の断定へ畳まない。**

- **確信の階級を字面に出す。** 「（実測 YYYY-MM-DD、対象: <系>）」「（人間の決定 YYYY-MM-DD）」「（docs の読みであって実測ではない）」を閉じに置く。⚠ **読み手が「誰が真にしている事実か」を字面から読み取れなければ、地雷は地雷に見えない。**
- **再測できるなら、その 1 行を添える。** 5 秒で確かめ直せる観測は、地雷ではなくセンサーである。⚠ **再測できない観測は、そう明示する** —— 確かめ直せないものを、確かめ直せるものと同じ顔で置かない。
- **これは aim に限らない。** aim の body・baton・PR・commit message・code コメント・doc —— **記録として残るものすべてに効く。**
- ⚠ **「確認済み」と書く前に、実際に走らせる。** 検証は書いた瞬間に真になるものではない —— ⚠ **未実施の検証を記録に書けば、後から読む者はそれを検証済みの事実として読む** ∴ **残っていたこと自体より、確かめずに断定したことのほうが重い**（2026-09-05 に踏んだ）。

## 「無い」と結論する前に —— 探索の射程を先に述べる

⚠ **「見つからなかった」は*世界*についての主張ではなく、*自分が見た範囲*についての主張である。** ∴ 射程を述べずに「無い」と書けば、**欠落がそのまま確信に化ける。**

- 🔴 **参照を書くとき・参照を信じて何かを消すときは、参照先の中身を見る。** 「あちらに書いてある」は**書いた時点でも真とは限らず**、後から中身だけ痩せることもある。
- **陽性対照を置く** —— その手段で「在ると分かっているもの」を検出できるか先に確かめる。**検出できないなら、測っているのは対象ではなく道具である。**
- ⚠ **探索の範囲を、対象が住みうる場所より狭く取らない。** 「参照されていない」は、どこを見たかに完全に依存する。
- ⚠ **`git status` が clean であることは、最新であることではない。** clean は*手元*についての主張であり、remote については何も述べていない。**消す前・書き換える前に `git fetch` を打つ。**

⚠ **この節は `td-apps-site` の `CLAUDE.md` から採った**（人間の決定 2026-09-05）—— **無関係の 2 repo が同じ過ちに当たり、片方は既に予防を書いていた。** ⚠ **規律は道具や領域を横断して持ち運べる**（同じことを、外側の領域から独立に述べた観測が `td-apps-site` の調査ノートに在る）。

⚠ **採用の直接の理由は、この 3 つを同じ日に踏んだことである**（2026-09-05、いずれもこのエージェント）:

1. **`docs/aims/_guide/` を「どこからも参照されていない」と報告して消した** —— 参照の有無を `docs/aims/` の中だけで調べ、**`@import` が住んでいた repo root の `CLAUDE.md` を見ていなかった** ∴ 2 repo で import を壊し、push した。⚠ **壊れ方は、当の `CLAUDE.md` 自身が警告していたものだった**（「`@path` の import は解決できなくても何も言わずに素通りする」）—— **画面には何も出ないまま、規律だけが context から抜ける。**
2. **1 か月遅れの checkout の上で「今日この file を ignore から外した」と報告した** —— `git status` が clean なのを「最新」と読んだ。**実際は 2026-08-04 に同じ変更が済んでいた。**
3. **commit message に未実施の検証を書いた**（「参照は 1 つも残らない。`git grep` で確認済み」）—— 実際には生きたリンクが 1 本残っていた。**追記で訂正し、history は書き換えていない。**
