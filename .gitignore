node_modules/
.DS_Store

# ⚠ `.claude/` は既定 deny —— 載せるものだけを名指しで開ける。
# 逆にすると、次にここへ増えたものが黙って公開される（2026-09-02、調査ノートが実際にそうなった。
# `.claude/` を丸ごと ignore している他の repo では偶然守られており、settings.json を tracked に
# する理由を持つこの repo でだけ毛布が無かった）。`scripts/classify-paths.mjs` が push の判定で
# 採っているのと同じ原則である —— **知らないものは安全側へ倒す。**
#
# ⚠ 2026-09-04、名指しで開けていた 1 つ（`settings.json`）が消えた。tracked にしていた理由は
# 「clone しただけで project スコープの plugin が有効になる」ことだけであり、供給を user スコープ
# 1 本へ寄せた時点で理由ごと無くなった（手順は README の「開発」節が持つ）。∴ 例外は 0 個で、
# `.claude/` は丸ごと ignore される —— **次に開けるときも、名指しで 1 行ずつ開けること。**
.claude/*

