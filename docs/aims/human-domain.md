---
aim: 人間は自分の領域である目的の木を、構造のまま見て、エージェントを介さずに動かせる
parent: bearing
state: open
---

# IS

**壊れているのは可視性ではなく、所有と可視性の逆立ちである。** 木は `parent:` の中にしか無く、`docs/aims/` は平らに並ぶ。⚠ **エージェントは全 file を読めば木を再構成でき、実際に毎セッションそうしている** —— できないのは、**その木を所有している人間のほうである。** [[aim-tree]] は「人間**だけが**動かせる不動点」と述べるが、⚠ **見えないものは動かせない** ∴ あの宣言は今のところ名目に留まる。本 node が埋めるのはその落差であって、新しい能力ではない。

**面は 2 つを同時に行う。** 木を構造のまま描くことと、**人間の欄だけを編集可能にすること**（`aim` / `parent` / `state`）。⚠ **後者は利便ではない。** canon は「frontmatter は人間のもの、body はあなたのもの」と定めているが、⚠ **今この境界を守っているのは、エージェントが指示に従っているという事実だけである** —— file の上では `state:` の 1 行と body は同じ md に同居し、区別は**書かれた法にしか存在しない**。面は同じ線を反対側から挟む: **人間には frontmatter しか触れない面、エージェントには法だけがある file。**

⚠ **費用は実演されている**（2026-09-03）。人間が「[[native-language]] は観測完了」と述べたとき、着地までに 3 手を要した —— エージェントが手を止め、`done` か `open` かを問い、選択を転記した。⚠ **うち 2 手はエージェントの介在であり、そこは宣言を取り違えうる経路でもある。** `aim:` の「エージェントを介さずに」は、この 3 手を 1 つの act へ畳むことを指している。

**手段は、静的な HTML 1 枚と File System Access API である。** ⚠ **build も依存も server も常駐 process も要らない** —— [[bearing]] は「build 手順も無い」を理由に npm を第 2 の配布経路から外し、YAML library も拒んでいる ∴ **React も vite も同じ理由で採れない。** ⚠ **そして corpus は browser 自身が読む ∴ 面は生成物ですらなく、置いてある 1 枚でよい** —— どの repo の `docs/aims/` を指しても動く。

**⚠ 実現可能性は推定ではなく実測である**（2026-09-03、Chrome 140、`file://`、headed、専用プロファイル）:

| 測ったこと | 結果 |
| :-- | :-- |
| `isSecureContext` ・ FSA API の在否 | true ・ 在る |
| `mode:'readwrite'` での pick | **ダイアログ 1 回で `granted`**（2 段にならない） |
| 列挙・読み取り | 8 node。**frontmatter が日本語ごと正しく読めた** |
| **書き込み** | 書いて読み直して一致 |
| **IndexedDB（`file://`）** | **生きている** ∴ handle を持ち越せる |
| 同一セッション内の reload | **`granted` のまま。クリック 0 回で書けた** |
| **ブラウザ再起動をまたぐ** | handle は**生存**、許可は **`prompt` へ落ちる** |
| 落ちた許可の回復 | **1 クリックで `granted`。ディレクトリの選び直しは不要** |

∴ **日常の費用は「起動後の最初の 1 クリック」だけである。**

⚠ **推定は 2 度外れた** —— 「`file://` では FSA が使えない」も「静的 HTML では編集が閉じない」も、測れば偽だった。**どちらも docs の記憶から述べたものである。**

⚠ **前身は同じ問題を別の答えで解いていた。** tmai の "offline" mode は `file://` ではなく **vite dev server（port 1421）**の上に在り、"backendless" とは *engine（HTTP backend）を外した*という意味だった —— **serverless ではない。** ⚠ **∴ あちらの形は、我々が測った道が塞がっていたことの証拠ではない。**

**前身から引き継ぐのは 3 つの設計判断であって、code ではない。**

1. **書き戻しは `aim` / `parent` / `state` だけを書き換え、他の frontmatter 行と body を byte 単位で保存する**（`parent` が消えれば行ごと落として re-root、新設なら `aim:` の直後へ）。⚠ **canon の境界が、そのまま code になっている形である。**
2. ⚠ **YAML library を使わない** —— 単一行 scalar の往復を手で書く。**前身も独立に同じ答えへ着いていた**（corpus の frontmatter 値は常に 1 行ゆえ、完全な emitter は要らない）。
3. slug の律（lowercase kebab・`--` 無し・先頭末尾 `-` 無し・**日付形でない**）は canon と一致する。

⚠ **引き継がないものが 1 つある。** あちらの format module は Rust engine の write surface の "faithful TS mirror" であり、**同じ契約が 2 言語に住んでいる。** 本 repo に engine は無く node 1 つ ∴ **この drift 源は最初から存在しない** —— ここは真似る対象ではない。

⚠ **その契約をどこに置くかは、測って 1 つに絞られた**（実測 2026-09-04、Google Chrome 140.0.7339.207、`file://`、既定フラグ、headless=new と headed の**両方**）—— `import('./x.mjs')`・`<script type="module" src>`・`fetch('./x.mjs')` の **3 つとも TypeError で塞がれた**。∴ 契約を別 file へ出すには生成が要り、⚠ **生成すれば「正本と生成物が一致しているか」を見る門が新たに要る** —— 腐りうる対が 1 つ増える。**HTML の中へ直に置けば、その門ごと不要になる** ∴ 契約は面の中に住み、**test はそこから取り出して走らせる**（複製しない）。⚠ **これは偶然にも、上段が「引き継がない」と名指した*同じ契約が 2 箇所に住む*形を、構造的に塞いでいる** —— 前身は同期を約束で守っていたが、ここには守るべき対が無い。⚠ **測ったのは既定フラグでの話である** —— `--allow-file-access-from-files` を渡せば変わりうるが、**面は人間がそのまま開くもの**であり、フラグを要求する道は採れない。⚠ **再測は安い**: 相対 import を 1 つ書いた HTML を `file://` で開けば済む。

⚠ **面へ辿り着く手段は、shim では解けない**（2026-09-05）。[[ambient-display]] の shim は*走る* ∴ 走るたびに install record を読んで今の版へ橋渡しできるが、**browser は HTML を開くだけであり、開かれた HTML は自分がどの版かを解決できない** —— 上段で測ったとおり `file://` では相対 import も `<script src>` も `fetch` も塞がれている。∴ **残るのは、home の固定 path への複製である**（`/bearing:setup-surface` が `~/.claude/bearing-aim.html` へ置く）。⚠ **これは本 node が既に述べていた形の帰結でしかない** —— 「面は生成物ですらなく、置いてある 1 枚でよい」。

⚠ **複製ゆえの費用を隠さない。** shim は自分で今の版へ橋渡しするが、**この 1 枚は 2 回の setup の間ずっと黙って古い。** ∴ `--check` が「置かれた 1 枚は今の面と byte 同一か」を答える。⚠ **違ったとき「古い版」か「人間が手を入れた」かは分けられない** —— 分けるには台帳が要り、それは [[adoption-declaration]] の `# HISTORY` が棄却した形である ∴ **分けられないことを述べるほうを採った。**

⚠ **開くのは人間である。** CLI は `file://` の URL を述べるだけで browser を起動しない —— **どの browser で、どの path 形で開くかは実際に効く**（上段の UNC の観測）∴ **我々が選べば、通らない側を黙って選びうる。**

⚠ **書き手が 2 人になる。** 人間が面で `state:` を回している最中に、エージェントが同じ node の body を書いている場面は現実に起きる。frontmatter しか書かないことは衝突を減らすが消しはしない ∴ **書き戻す前に読み直し、変わっていれば書かずに述べて止まる** —— `/bearing:setup-statusline` が既存の面を上書きせず止まるのと同じ律である。

⚠ **到達範囲は Chromium 系に限られる。これは我々の実測ではない。** FSA の対応状況から引き継いだ前提であり、前身も同じものを飲んで「Chrome / Edge で開いてください」と述べている。⚠ **測っていないことを、測ったことのように書かない。**

⚠ **到達範囲は browser の種類だけで決まらない —— *どの path 形で面を開き、どの path を選ぶか*も効く**（人間の観測 2026-09-04、Windows の Chrome）。`\\wsl.localhost\…` 越しに面を開き、同じ share の中の corpus を選ぼうとしたところ、**Chrome が「システムフォルダが含まれている」と拒んだ**。⚠ **理由は測っていない** —— 判定が UNC そのものに掛かるのか、その先の path に掛かるのかを分ける観測を持たない ∴ **述べられるのは「この経路では拒まれた」までである。** ⚠ **回避は在り、観測された**（人間の観測 2026-09-04、同一機体）: WSL 側の Chrome（WSLg）から `file:///home/…/surface/aim.html` を開き、Linux の path（`/home/…/docs/aims`）を選ぶと **picker は拒まず、corpus 10 枚すべてが往復一致と出た**。⚠ **「UNC を通らないことが効いている」は読みであって測っていない** —— 分けるには、*同じ Linux path を Windows の Chrome から選ぶ*対照が要る。∴ 言えるのは **2 つの経路のうち一方は通り、他方は拒まれた**までである。

⚠ **そして拒否は browser 自身のダイアログにしか出ず、面には `AbortError` として届く** ∴ **人間が取り消した場合と字面で区別がつかない。** ⚠ **面がそこで黙れば、ダイアログを閉じた瞬間に「拒まれた」という事実が画面から消える** —— [[ambient-display]] の「消えたものと元から無いものが同じ見た目になる」の、この面における形である。∴ **どちらかに決めつけず、両方が来る欄だと述べる。**

# PROCESS

- [done] **実現可能性を実測した**（上の表）。⚠ **推定が 2 度外れた後に測っている** —— 順序が逆だったことは、この node の出自として残す
- [done] **frontmatter の往復を書き、test で固定した。** 契約は `carriers/claude/bearing/surface/aim.html` の `data-contract="aim-frontmatter"` block に住み、`test/claude/bearing/aim-format.test.mjs` が**その block を取り出して**走らせる。⚠ **`split('\n')` を 1 度も通らず、最初から最後まで index で切り貼りする** —— あれは改行の形と末尾改行の有無を落とし、[[bearing]] の `with-aim` の伸長はまさにそれだった。固定した対: LF ／ CRLF ／ 末尾改行なし ／ 末尾の空行 ／ body 中の `---` ／ 未知の frontmatter 行 ／ `:` の後ろの空白量 ／ `parent` の削除と新設（**CRLF の file なら挿入行も CRLF**）／ 引用の要否 ／ 改行を含む値の拒否。⚠ **実 corpus 全枚に対する恒等も門にした** —— 合成例だけでは、実際に置かれている形を守れない。⚠ **browser でも同一に動くことを実測した**（2026-09-04、Chrome 140、契約を載せた面を `file://` で開き、恒等 3 種を確認）—— **node で通ったことは、browser で通ったことではない**
- [done] **読み取り専用の検分面を置いた。** `docs/aims/` を選ぶと各 node の 3 欄を読み、**同じ値で書き戻して 1 byte も動かないか**を 1 枚ずつ述べる。⚠ **書き込みはまだ 1 行も無い** —— 往復が*人間自身の corpus の上で*恒等であることを、**人間が観測できるようにする**のが先である。⚠ **「読めなかった」を「親が無い」に畳まない**（verdict は 3 値）—— picker だけを stub して描画の経路を実際に走らせたとき、**最初の実装は frontmatter を持たない file を `（root）` と描いていた**。この node 自身が [[ambient-display]] から引いて warn している形を、この node の実装が踏んだ
- [done] **`parent:` から木を組み、壊れた形を畳まずに述べるようにした。** 正本は面の中の**第 2 の契約 block**（`data-contract="aim-tree"`）で、`test/claude/bearing/aim-tree-shape.test.mjs` が**その block を面から取り出して**走らせる ∴ 契約が 2 箇所に住むことは構造的に起こりえない（`aim-frontmatter` と同じ形・同じ理由）。🔴 **壊れ方は 4 つを別の名で述べる** —— `self`（自己参照）/ `missing-parent`（存在しない親）/ `cycle`（循環）/ `unreachable`（root から辿り着けない）。⚠ **`unreachable` は他の 3 つと重ねて出す** —— **原因と症状はどちらも消さない。** ⚠ **root が複数あることは述べるが咎めない** ——「forest は誤りか」は目的の問いであり、契約は答えない。⚠ **「親が空欄」と「親が居ない」を同じ顔にしない**（前者は root、後者は壊れ）。✅ **この repo の corpus に対して実際に組んだ**（実測 2026-09-06）—— **problem 0 件・root 1 本**。陽性対照つき（corpus を 1 枚も読めていなければ落ちる）
- [done] **面を 1 枚の HTML にした。** 木の描画（state を添える）・body の表示（**読むだけ**）・frontmatter の編集（`aim` は textarea、`parent` は corpus からの選択肢、`state:` は open/done/dead の選択肢）。⚠ **body は編集させない** —— あれはエージェントの領分である。⚠ **読めない file は 3 欄ごと編集不可にした** —— 空欄で見せれば、書き戻した瞬間に**「読めなかった」が「空だった」として disk へ落ちる。** ⚠ **corpus に無い親は選択肢として残す** —— 黙って root へ倒せば、**壊れた参照が「元から root だった」ことにされる。** ⚠ **root から辿り着けない node も木の下に描く** —— 木に出ないだけでは、消えたのか元から無いのか区別できない。⚠ **picker は最初から `readwrite` で訊く** —— 読み権限で開いてから書けば、**人間が編集を終えた後に拒まれる**のが最も高くつく。

  ✅ **2026-09-13、画面が初めて観測された**（人間の観測、この機体）—— **木の描画**と**人間が操作できる frontmatter 部**を確認した。🔴 **`# OBSERVATION` の 1 票目が、これで初めて*問える*状態になった** —— それまで機構が主張していたのは「parse できる」「id が実在する」の 2 つだけだった。

  ⚠ **書き戻しは行われていない**（人間の判断 2026-09-13: 「**実際の書き戻しは必要なタイミングで行います**」）∴ 🔴 **2 票目（面から `state:` を書き換え、file が実際に動く）は未観測のままである** —— **往復の恒等を固定しているのは*契約 block* についての test であって、この corpus の上の実際の書き込みではない。** ⚠ **3 票目（同時書き込みの拒否）も同じく未観測** —— あれは失敗経路であり、**正常系をいくら見ても観測されない。**
- [done] 🔴 **面の script を parse する門を置いた**（`test/claude/bearing/surface-shape.test.mjs`）。⚠ **`node --test` はこの面の UI を 1 行も走らせていなかった** —— 契約 block は取り出して走らせていたが、**画面を組む側は誰も parse しておらず、syntax error を入れても suite は緑のまま通り、壊れるのは人間が開いた瞬間で、しかも画面には何も出ない。** 併せて **script が名指す id が HTML に実在するか**も検査する（在らない id を引けば `null` に書き込む形になり、console にしか出ない）。✅ **変異試験で確かめた**（実測 2026-09-06、id を 1 文字 typo させると赤、復元で緑）。⚠ **この門が答えるのは「parse できるか」と「id が在るか」の 2 つだけであり、それ以上を主張しない。**
- [done] **面へ辿り着く手段を、version で腐らない形にした。** `/bearing:setup-surface` が `~/.claude/bearing-aim.html` へ面を置き、**人間はその 1 つの path を bookmark する** —— cache path は version を含み cache は旧版を消さないが、**この path には版が入らない。** ⚠ **shim ではなく複製である**（`# IS`）—— browser は開くだけで、開かれた HTML は自分の版を解決できない。⚠ **毎回置き直し、`--check` が今のかを答え、違う理由は 1 つに決めつけない。** ⚠ **開かない** —— path 形が効く以上、我々が選べば通らない側を黙って選びうる。`test/claude/bearing/setup-surface.test.mjs` の 8 件と、`scripts/consumer-check.mjs` の「置き先が版を含まない」が固定する。⚠ **2026-09-05、置いてから開くまでが実際に通った**（人間の観測、この機体）—— `bearing-setup-surface.mjs` が `~/.claude/bearing-aim.html` へ置き（直前まで `--check` は「**置かれていない**」と答えていた）、**WSL 側の Chrome（WSLg、`DISPLAY=:0`）からその `file://` を開き**、`/home/trustdelta/works/bearing/docs/aims` を選んで **corpus 全 11 枚が往復一致**と出た。⚠ **前日の観測との差は、開いたのが checkout の中の面ではなく*置かれた複製*だという 1 点である** —— 往復そのものは 2026-09-04 に既に通っていた ∴ **新しく通ったのは「辿り着く手段」のほうであって、契約ではない。** ⚠ **1 機体・1 browser の観測である。** 再測は `bearing-setup-surface.mjs --check` と、同じ 2 手（開く・選ぶ）

  🔴 **2026-09-13、`# IS` が予言した費用が実地で現れた** —— **「この 1 枚は 2 回の setup の間ずっと黙って古い」が、観測を 9 日遅らせた。** 人間が面を開いて「aim が在ることが見える程度」と述べた時点で、置かれていたのは **`a4762a4`（2026-09-04）と byte 同一の 371 行**であり、正本は既に 730 行で body 表示・frontmatter 編集・同時書き込みの拒否を持っていた（実測 2026-09-13、対象: この機体の `~/.claude/bearing-aim.html`）。⚠ **面が持っていなかったのではなく、届いていなかった** —— **`--check` は違うと述べていたが、人間が打つまで誰も訊いていない。**

  ⚠ **この 1 件では、`# IS` の「古い版か人間の手か分けられない」の片方を実際に消せた** —— **面を履歴の各版と `cmp` したら `a4762a4` に一致した** ∴ 手は入っていない。🔴 **だがこれは手で行った照合であって、機構の能力ではない**（**再測**: `for sha in $(git log --format=%H -- carriers/claude/bearing/surface/aim.html); do git show $sha:<path> | cmp -s - ~/.claude/bearing-aim.html && echo $sha; done`）。⚠ **cache は 17 版を消さずに持っている ∴ checkout を持たない側でも同じ照合はできる** —— **これを `--check` へ入れる案は提案しただけで採られていない**（2026-09-13）∴ **`# IS` の主張は動かしていない。**
- [done] **同時書き込みで黙って上書きしないようにした。** 🔴 **書く前に必ず disk を読み直し、面が読んだ原文と 1 byte でも違えば、書かずに述べて止まる** —— 面を開いてからの間にエージェントがその file を動かしていれば、**握っている原文の上に書けばその変更は一言も出ずに消える。** 止まったあとは読み直した内容で面を組み直す ∴ 人間は**何が変わったかを見てから**やり直せる。⚠ **読み直せなかったときも書かない** ——「読めなかった」を「変わっていない」に畳まない。⚠ **`edit` が投げたときも、`next === fresh` のときも、1 byte も書かない**（後者は「変更はありません」と述べる）
- [done] **「不在を黙って示さない」を 3 つの経路で守った。** ⑴ FSA を持たない browser では、**空ではなく「開けない」と述べて止まる**。⑵ 読めない file を `（root）` と描かない（上の verdict 3 値）。⑶ **picker が拒まれたときも黙らない** —— ⚠ **拒否は browser のダイアログにしか出ず、面には `AbortError` として届く** ∴ 取り消しと区別がつかない。決めつけず、両方が来る欄だと述べる。⚠ **これは 2026-09-04 に実際に拒否を踏んで分かった** —— それまで面はそこで**完全に黙っており**、人間はダイアログを閉じた後、何も起きない面を見ていた

- [done] 🔴 **面を 3 つに割り、切り方を*所有*にした**（人間の提案 2026-09-13 —— 逐語: 「**提案ですが、タブ化するのはどうでしょう　tree human agentの3つです（名は後ほど）。treeは今の見せ方。humanはESCALATIONとOBSERVATIONを見せます。agentではtodoをみせます**」）。**木**（構造と、人間が持つ 3 欄）／**人間の番**（`# ESCALATION` と `# OBSERVATION`）／**エージェントの残務**（`[todo]`）。⚠ **これは表示の整理ではなく、この node の `aim:` の分割そのものである** —— 🔴 **`lib/process.mjs` が既に「観測を可能にする作業は PROCESS、判断そのものは ESCALATION、観測と宣言は人間」と 3 つに分けており、面はその分割を初めて画面に写した。** ⚠ **タブの名は HTML の 3 行にしか無い**（人間が後で決める）。

  ⚠ **並べ替えない。** 🔴 **どれが重いかを述べることは注意予算の割り当てであって観測ではない** —— `open-todo` と `escalation` に課されているのと同じ規律が、面にも同じく効く ∴ **slug 順で並べ、絞るのは人間に任せる。** ⚠ **card の slug は押せる** —— 押せば木の面でその node が開く（**番を渡すことと動かせることは別である**: `aim:` は「エージェントを介さずに**動かせる**」と述べている）。

  🔴 **body の parse は、この面にとって 2 つ目の実装である。** `lib/process.mjs` が同じ節と同じ mark を parse し、**人間が既に見ている数**（`open-todo` / `escalation` / `observation`）を出している。⚠ **`file://` は相対 import を塞いでいる**（実測 2026-09-04、対象: Google Chrome 140.0.7339.207、`file://`、既定フラグ）∴ 面があちらを読む道は無く、生成すれば「正本と生成物が一致しているか」の門が要る —— **この node が既に棄却した形である。** ∴ **採ったのは「2 つ在ることを認め、食い違いを門にする」**: `test/claude/bearing/aim-body-shape.test.mjs` が **実 corpus 全枚で両者の出力を `deepEqual` で突き合わせ**、`bodyOf` の切り方も別軸で突き合わせる。⚠ **これは「同じ code である」ことの保証ではない** —— **両方が同じように取り落とす body の形は、corpus に現れるまで検出されない。**

  🔴 **描画そのものを走らせる門を初めて置いた**（`test/claude/bearing/surface-render.test.mjs`）。⚠ **`surface-shape` が答えるのは「parse できるか」「id が在るか」の 2 つだけであり、描画の中で数を取り違えても緑のまま通る。** ⚠ **この repo は一度それを踏んでいる** —— 上の `[done]` が書いている `（root）` の bug は「picker だけを stub して描画の経路を実際に走らせた」ときに見つかったが、**その stub は commit されておらず、次の面が同じ穴を持って生まれた。** ∴ **document を stub して `renderHuman` / `renderAgent` / `showTab` を実際に呼び、面の数が `gatherBacklog` の数と一致することを assert する。** ⚠ **stub は browser ではない** —— 固定できるのは*論理*であって*見え方*ではなく、**ここが緑でも開いて何も出ないことは在りうる。**

  ⚠ **不在を黙って示さない側を、新しい面でも守った**: 見出しだけの節を「在る」と数えず**述べる**／読めない file を空欄で並べず**何件落としたかを述べる**／`[todo]` 0 件を「尽くした」と読ませない／**`# ESCALATION` と `# OBSERVATION` が 0 件でも「番が無い」と読ませない**（**この面は `state:` の宣言待ちを数えていない**）。🔴 **mark に見えて数えられなかった行も面に出す** —— **黙って落とせば、面と statusline が同じ corpus について別の数を述べる**（[[surface-parity]] が名指した形）。

  ✅ **変異試験 11/11 検出、陰性対照緑**（契約 6: fence の中を数える／空の見出しを中身在りにする／散文を票と数える／`bodyOf` が frontmatter を残す／項目の 1 行目を落とす／anomaly を捨てる。面 4: 読めない file を黙って落とす／anomaly を画面から落とす／tab が全部見せる／0 件の但し書きを落とす。HTML 1: pane を 1 つ消す）。**再測**: `node --test`（実測 2026-09-13 に **554 本緑**）／`bash scripts/carrier-check.sh`／`node scripts/consumer-check.mjs`（いずれも exit 0）。

  ✅ **2026-09-13、3 面すべての表示が観測された**（人間の観測、この機体 —— 逐語: 「**3タブとも正常な表示を確認しました**」）。🔴 **だが観測されたのは*描かれたこと*であって、*番の受け渡しとして働いたこと*ではない** —— ⚠ **`# OBSERVATION` に足した票の 2 点（ESCALATION が頭から読み直さずに判断できる形か／残務に人間待ちが混ざっていないか）は、どちらも未観測のままである。** **表示の確認を、票の充足として読まない。**


- [done] 🔴 **字を読み手の側へ返した**（人間の観測 2026-09-13 —— 逐語: 「**1点、全体に言えることですが、フォントが読みにくいです**」）。⚠ **測ったら、原因は 2 つとも「小さい」ではなく*構造*だった:**

  🔴 **⑴ body が絶対 px で字を決めていた**（`font: 15px/1.7`）—— **読み手が browser に設定した大きさを上書きする。** ⚠ **しかも他はすべて `rem` である** ＝ `rem` は `html` を見る ∴ **読み手の設定に従う部分と従わない部分が 1 枚の中で混ざっていた**（本文 15px、二次の字は読み手の設定の 0.8〜0.875 倍）。∴ **絶対 px を捨てた**（`1rem`）—— **大きくしたのではなく、決めるのをやめた。**

  🔴 **⑵ `pre` が `font-family` を持たず、UA 既定の monospace で描かれていた** —— ⚠ **`pre#d-body` に入るのは node の body、つまりこの面でいちばん長い日本語の散文である。** 節の原文（`pre.sec`）も同じ。∴ **どちらにも本文の family を明示し、`code` も既定任せをやめた**（既定は本文より一段小さく描かれる）。

  ⚠ **contrast は原因ではない**（実測 2026-09-13、対象: この面の 5 組 —— muted が light **5.36:1** / dark **6.07:1**、fg が **16.62** / **14.63**、いずれも AA 通過）∴ **色は 1 つも動かしていない。** 🔴 **測らずに触れば、原因でないものを直して原因を残す。** **再測**: `node -e` で相対輝度から比を出す（`:root` の 6 変数）。

  ⚠ **どの face が実際に選ばれるかは測っていない** —— fontconfig と install 済みの font に依り、**我々には見えない。** ⚠ **`--font` に在るのは*並び*であって、観測ではない。**

  ✅ **門を置いた**（`surface-shape.test.mjs`）: **字を絶対 px で決めていないこと**／**本文の 0.875 倍より小さい字が無いこと**／**`pre#d-body` と `pre.sec` が family を明示していること**。⚠ **comment を落としてから測る** —— **過去の値は理由として字面に残っており、落とさなければ直した当の記述が違反として出る。** ✅ **変異試験 4/4 検出**（px へ戻す／2 つの `pre` から family を落とす／字を `.84rem` へ戻す）、陰性対照緑。⚠ **これは heuristic である** —— **見ているのは CSS の字面で、描かれた結果は browser にしか無い。** 固定したのは「二度と同じ形へ戻さない」ことだけである。

  ✅ **2026-09-13、効いたことが観測された**（人間の観測、この機体 —— 逐語: 「**各段に読みやすくなりました**」）。⚠ **票の 2 点は個別には述べられていない** —— **「読みやすくなった」は観測であり、「node の body が monospace でなくなった」と「browser の文字サイズ設定に従う」は、そこから*含意される*だけである** ∴ **含意を観測として記さない。**


- [done] 🔴 **`aim:` 文を、人間の番とエージェントの残務の両面に入れた**（人間の観測 2026-09-13 —— 逐語: 「**ESCALATIONの中身がnodeを頭から読み直さずに判断できるかですが、aim:文がないと厳しいかもです**」）。⚠ **slug と `state:` だけでは、「何に対する escalation か」が画面に無い** —— 🔴 **判断は目的に対して下される。** ⚠ **残務の側にも入れた** —— **同じ理由が効く**（その todo がまだ要るかは、目的に照らして決まる）。⚠ **`aim:` を持たない record では黙らない** —— 空行で見せれば**「目的の無い node」が「目的を書き忘れた node」と同じ顔になる** ∴ **壊れとして述べる。** ✅ **変異試験 2/2 検出**（`aim:` を出さない／無いときに黙る）。**再測**: `node --test test/claude/bearing/surface-render.test.mjs`（実測 2026-09-13 に 11 本緑）。

  🔴 **同じ観測が、面では閉じられないものを 1 つ名指した** —— **「ISあってこそでもありますが、ISは現状ボリュームが問題ですね」。** ⚠ **面は body の書き方を直せない** ∴ **[[aim-tree]] の `# ESCALATION` ⑵ へ上げた**（記録の形の問題であって、表示の問題ではない）。

- [done] 🔴 **「エージェントの残務」の面が、法の穴を*見える*ものに変えた**（人間の観測 2026-09-13 —— 逐語: 「**混ざってはいますが、混ざっていると認識できるようになったことが意味を生みます**」）。⚠ **これは本 node の達成ではなく、[[aim-tree]] の `# ESCALATION` ⑴ の材料である** —— **面が変えたのは*見えるかどうか*であって、混ざりそのものではない。** 🔴 **`CLAUDE.md` の尺で言えば、気づけない漏れが気づける漏れに変わった** ∴ **安くなったが、無くなっていない。** ⚠ **どれが人間待ちかは面も当方も判定していない** —— **それはあちらの escalation が求めている判断である。**


# OBSERVATION

- 🔴 **`surface/aim.html` を browser で開き、木が描かれることを見る。** ⚠ **機構が主張しているのは「parse できる」「script が名指す id が実在する」の 2 つだけであり、画面は 1 度も観測されていない**（picker が人間の操作を要する）。
- **面から `state:` を書き換え、file が実際に動くことを見る。** ⚠ **往復の恒等は test が固定しているが、それは*契約 block* についてであって、*あなたの corpus の上の実際の書き込み*ではない。**
- **同時書き込みの拒否が実際に発火することを見る** —— 面を開いたままエージェントに同じ node を触らせ、面が**書かずに述べて止まる**か。⚠ **これは失敗経路であり、正常系をいくら見ても観測されない。**
- 🔴 **字が読めるようになったか。** ⚠ **機構が固定できるのは「絶対 px で決めていない」「下限を割らない」「`pre` が family を明示している」の 3 つだけであり**（`surface-shape.test.mjs`）、**読めるかどうかは開いた人間にしか無い。** ⚠ **特に見るべきは 2 つ**: ⑴ **node の body（いちばん長い日本語の散文）が、monospace ではない字で描かれているか** ⑵ **browser の文字サイズ設定を変えたとき、面がそれに従うか** —— 🔴 **従わなければ、絶対 px がどこかに残っている。**
- 🔴 **3 つの面に分かれた切り方が、番の受け渡しとして実際に働いたか。** ⚠ **機構が主張できるのは「面の数が `gatherBacklog` の数と一致する」までであり**（`surface-render.test.mjs`）、**「人間の番に何が在るかが、読んで分かる」は画面にしか無い。** ⚠ **特に見るべきは 2 つ**: ⑴ **`# ESCALATION` の中身が、node を頭から読み直さずに判断できる形で出ているか**（出ていなければ、面は番を渡しただけで材料を渡していない）⑵ **`エージェントの残務` に人間待ちが混ざっていないか**（混ざっていれば、それは面の誤りではなく `[todo]` の法の穴であり、[[aim-tree]] の `# ESCALATION` ⑴ が既に上げている）。
- **エージェントを介した 3 手（手を止める → 問う → 転記する）より、実際に少ない手数で倒せたか。** ⚠ **`aim:` が言う「エージェントを介さずに」は、この 3 手を 1 つの act へ畳むことを指している** —— **畳めたかを述べられるのは、実際に倒した人間だけである。**

# DAG

- 関連: [[aim-tree]] —— 本 node が埋めるのは、あちらの**名目と実質の落差**である。⚠ **あちらの手段（所有で目的を引く・置き場は repo・木で構造化する）には一切触れていない** ∴ 面が 1 つ増えても、あちらに書き換える主張は無い
- 関連: [[ambient-display]] —— どちらも面だが、あちらの `aim:` は「対象ハーネスが**提供する可能範囲内**で」と自らを限定している ∴ **browser はあちらの手段になりえない。** 版の腐りと「不在を消さない」の 2 つは、あちらの前例に倣う
- 関連: [[surface-parity]] —— ⚠ **あちらの `[todo]`（デスクトップ側から aim へ届く経路）を、本 node は閉じない。** あちらの「デスクトップ」は Claude Desktop の Code ＝ *起動経路*の話であり、本 node はどの起動経路にも属さない**セッションの外の面**である。**2 つを混同しないこと**
- 関連: [[adoption-declaration]] —— ⚠ **あの gate は本 node に掛からない。** 採用の宣言が要求されるのは**黙って走るもの**に対してであり、面は人間が開いて自分で書き換える act ∴ 手で md を編集するのと同じ層に立つ。痕跡は残るが、残すのは人間自身の編集である。⚠ **面を置く先が home になったことで、理由はさらに強くなった**（2026-09-05）—— `/bearing:setup-surface` は repo に 1 byte も書かない ∴ あの軸（「痕跡が残るか」）で測っても、要求すべき宣言は無い
- 照合: [[aim-tree]] @ b1fad82662fb —— あちらの手段は「所有で目的を引く・置き場は repo・木で構造化する」であり、**その木を誰がどう見るか**には触れていない ∴ 本 node が面を足しても、あちらに書き換える主張は無い
- 照合: [[ambient-display]] @ b1fad82662fb —— あちらは `aim:` の側で「ハーネスが**提供する可能範囲内**」と自らを限定している ∴ browser の面はその外に立ち、あちらの手段は 1 つも動かない
- 照合: [[surface-parity]] @ b1fad82662fb —— あちらが測る非対称は**起動経路の間**のものであり、本 node はどの経路にも属さない ∴ あちらの表も `[todo]` も動かない（⚠ **閉じないことのほうが重要である**）
- 照合: [[adoption-declaration]] @ b1fad82662fb —— あの法の gate は**黙って走るもの**に掛かる。面は人間が開く act ∴ 例外を足す必要も、軸を変える必要も無い
- 照合: [[native-language]] @ b1fad82662fb —— 本 node は面の文言を母語で書く側であり、あちらの判別線（人が読む文か、機械が parse する token か）をそのまま使う ∴ 動かす主張は無い
- 照合: [[bearing]] @ b1fad82662fb —— 親。⚠ **3 柱を 1 つも増やしていない**（[[aim-tree]] の到達を助ける面であって、新しい柱ではない）∴ `aim:` に触れない。手段が引いている「build 手順も無い」は、あちらに既に在る前提の**利用**であって追加ではない
