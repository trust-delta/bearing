---
aim: bearingが何を目指し、どのような機構で達成しようとしているか。何を要求されてどのようなactが残されるのか。採用を検討する人に伝える面をもつ
parent: bearing
state: open
---

# IS

**この面が述べるのは*契約*であって、corpus の要約ではない。** 🔴 **要約は複製であり、複製した側が先に古くなる** —— この repo が一貫して拒んできた形である（`CLAUDE.md` の記録の作法、[[aim-tree]] の canon 1 正本、handoff の `read.md` / `write.md` が別名 command に手順を持たせない理由）。⚠ **∴ corpus を畳んで「入門」を作る道は採らない。**

**代わりに述べるのは、corpus のどこにも 1 箇所には書かれていないものである** —— 🔴 **採ると何が repo に置かれ、その後どの act が人間に残り続けるか。** ⚠ **今それは散っている**: [[adoption-declaration]] は*機構*として痕跡の残る側にだけ宣言を要求し、[[aim-tree]] は所有の非対称を規定し、[[human-domain]] はそれを人間が動かす面を述べる —— **どれも「採ろうとしている人が、採る前に読むもの」としては書かれていない。** ∴ **これは要約ではなく、新しく述べるべきものである。**

⚠ **欠落は 2 つ、実際に測った**（実測 2026-09-09、対象: `README.md`(173 行) / `README.en.md` / `CONTRIBUTING.md`。**再測は `grep` 2 本**）:

⑴ **「aim とは何か」を人間へ説明したものが無い。** README が持つのは表の **1 行**だけで、node の節（`# IS` / `# PROCESS` / `# OBSERVATION` / `[todo]`）は **1 度も現れない**（grep 0 件）。⚠ **実質の説明は `aim-authoring.md` だが、あれはエージェント宛の指示であり、しかも `/bearing:setup-aim` が消費者の repo へ*置く*ものである** ∴ **採用を決める前の人間が読む面としては、存在しない。**

⑵ **面に毎ターン出る 4 つの数の意味が、どこにも書かれていない** —— `todo` / `宣言待ち` / `escalation` / `観測票なし`（grep 0 件）。🔴 **あれは「人間の番がいくつ渡っているか」を述べる数である** ∴ **意味を知らない読み手の画面では、ただ数字が 4 つ並んでいるだけになる。**

**∴ 手段は 4 つで決まる。**

**⑴ 置き場は README ではなく、別の 1 枚にする。** ⚠ **README は既に導入・採用・装着・更新の*参照*として厚い** ∴ **そこへ契約を混ぜれば、毎回引かれる手順書の中に、1 度しか読まれないものが埋もれる。** README からは 1 行で導き、**README 自身は手順の面のままにする。**

**⑵ 版で腐る字を持たせない。** ⚠ **node 数・版番号・その日の count は動く** ∴ **literal で書けば書いた瞬間から古くなり、しかも黙って古くなる。** **述べるのは構造と act であって、その日の値ではない。**

**⑶ 実例は引き写さず、指す。** ⚠ **入門が例を持たなければ読者は木を想像するしかないが、node を本文へ複製すれば ⑴ の禁（複製した側が先に古くなる）を自分で破る** ∴ **この repo 自身の corpus を*指す*。**

**⑷ 英語版を併置する。** ⚠ **判別線は既に決まっており、本 node が決めるものではない** —— [[native-language]] の「**読む面**は英語を併置し、**開発に加わる面**は日本語のみ」。**この面は読む面である。**

# PROCESS

- [done] **契約を 1 枚に畳んだ doc を書いた** —— `docs/adoption.md`。**置かれるものは表で述べ、🔴 リポジトリに残るのは 3 つだけ（`CLAUDE.md` の block・`.claude/skills/aim/`・corpus 自身）で、残りはすべて home の下だと明記した。** ⚠ **外し方も同じ表の直後に置いた** —— **置く手段だけを述べて外す手段を述べなければ、契約ではなく勧誘になる。** ⚠ **「何を要求されないか」の節も置いた**（作法を全部覚えなくてよい／木を先に設計しなくてよい／全部を採らなくてよい）—— **要求の一覧だけでは、読み手は要求されていないものまで要求と読む。** ⚠ **版で腐る字を持たないことを確認した**（`v0.x` 0 件・skill の枚数は「一式」へ。✅ 陽性対照: 同じ grep が `CLAUDE.md` では 1 件拾う）
- [done] **aim node の形を、読者が想像せずに済む最小限で述べた** —— 骨格（frontmatter 3 行 ＋ `# IS` ＋ `# PROCESS`）を 1 つ示し、⚠ **6 節が分けているのは*内容の種類*ではなく「誰の番か」だと表で述べた。** 🔴 **`[todo]` と `# OBSERVATION` の鏡像**（自力で確認できること／できないこと）と、**`[done]` は「実装された」であって「満たされた」ではない**ことを置いた。⚠ **canon は 1 文も写していない** —— 骨格は canon の「骨格」節と同じ形になるが、**それは*最小の node がどう見えるか*という事実であって、規則の複製ではない。** ⚠ **実物は指すだけにした**（`docs/aims/` と root の `bearing.md`。✅ **root であることを確認: `parent:` を持たない node は 11 中 1 つ、それが `bearing.md`**）
- [done] **面に出る数の意味を、同じ doc に置いた** —— ⚠ **軸は前節と同じ「誰の番か」にした**（`todo` はエージェント、`宣言待ち` と `escalation` は人間）。🔴 **3 つを同時に読むこと**（2 つだけで読めば、`# ESCALATION` で止まっている node が「何も渡っていない」に見える）と、**`観測票なし` だけは出たら機構側の欠陥である**ことを置いた。⚠ **「0 は描かれない」「`?` は 0 ではなく採れなかった」も書いた** —— **描かれないことを「事実が無い」と読ませない。** ⚠ **面がしないこと**（重さの順位づけ）**も明記した。** ✅ **label 5 つを `bin/statusline.mjs` の `counter(...)` と 1 字ずつ突き合わせた**（実測 2026-09-09）。⚠ **実数は 1 つも書いていない** —— `<数>` の placeholder（腐るため）
- [done] **README から 1 行で導いた** —— ⚠ **置き場は「これが何ではないか」の直後、`## 使い方` の直前**である。🔴 **順序が意味を持つ**: 採るかどうかの判断は手順の*前*に来る ∴ 使い方の中に置けば、**既に採ると決めた人しか通らない場所**になる。⚠ **blockquote は README が既に使っている形に揃えた**（「なぜこの形なのか」と同じ）—— **新しい装飾を持ち込めば、読み手はそれ自体を解釈する。**
- [done] **英語版を併置した** —— `docs/adoption.en.md`（8 節が日本語版と 1 対 1）。⚠ **冒頭に「これは翻訳であり、食い違えば日本語が正である」を置いた** —— `README.en.md` が既に採っている形。🔴 **面の label は訳さず、日本語のまま置いて英語の注釈を添えた**（`宣言待ち` (awaiting declaration) / `観測票なし` (no ballots)）—— ⚠ **statusline が描くのは日本語の字である ∴ 訳せば、画面に出ない語を探させることになる。** ⚠ **`README.en.md` からの導線も同時に足した** —— **片方だけ足せば、英語の読者にだけ入口が無い。**
- [done] **版で腐る字を持たないことを検査する門を置いた** —— `test/adoption-doc-rot.test.mjs`（4 本）。見張るのは 3 種だけ（**版番号・面の例に混ざった実数・node の個数**）で、⚠ **日付つきの実測は腐らない ∴ 対象外**である。✅ **陽性対照と偽陽性の門を両方置いた** —— 合成した違反を 3 種とも挙げること、そして placeholder・日付つき実測・「node 数」という語を 1 つも挙げないこと。✅ **変異試験を 2 つ当てた**（実測 2026-09-09）—— 本番の doc に版番号を混ぜる／面の例に実数を混ぜる。**2 つとも赤くなり、復元で緑に戻った。** 🔴 **「無ければ skip」を無条件にしなかった** —— ⚠ **消されたことと居場所が違うことが同じ顔になる** ∴ **bearing 自身の checkout に居るかを別の印（`scripts/carrier-check.sh` の存在）で判定し、居るなら不在は落とす。**

# DAG

- 関連: [[adoption-declaration]] —— あちらは**痕跡が残る側にだけ宣言を要求する機構**、本 node は**その宣言をする前の人間へ、何が置かれ何が残るかを述べる面**
- 関連: [[aim-tree]] —— 所有の非対称（`aim:` は人間、body はエージェント）は本 node が伝える契約の中心だが、⚠ **規定するのはあちらであって本 node ではない**
- 関連: [[human-domain]] —— あちらは**採った後に人間が木を動かす面**、本 node は**採る前に読む面**
- 関連: [[ambient-display]] —— **4 つの数を描くのはあちら**、⚠ **その意味を述べる面が無いことが本 node の欠落 ⑵ である**
- 関連: [[native-language]] —— 英語版の要否は、あちらの「読む面／加わる面」の判別線が既に決めている
- 照合: [[bearing]] @ f9c117c9 —— 親の `# IS` / `# PROCESS` は README を**install 手順の記述**としてしか扱っておらず、**対外の説明面を自分の手段として主張していない** ∴ 子が 1 つ増えても向こうに書き換える主張が無い
- 照合: [[adoption-declaration]] @ f9c117c9 —— 本 node はあちらの機構を**説明の題材にするだけ**で、**痕跡と宣言の線引きそのものには 1 文字も触れていない**
- 照合: [[aim-tree]] @ f9c117c9 —— 所有の非対称を**引用する側**であり、規定を動かしていない
- 照合: [[ambient-display]] @ f9c117c9 —— **4 つの数を描く**のはあちらの手段のままで、本 node は**その意味を述べる面が無いこと**を自分の欠落として引き受けた ∴ あちらに新しい手段は生じない
- 照合: [[human-domain]] @ f9c117c9 —— あちらは**採った後**、本 node は**採る前**であり、⚠ **時点が重ならない**
- 照合: [[native-language]] @ f9c117c9 —— 判別線を**適用しただけ**で線そのものを動かしていない。⚠ **あちらは `state: done`**（人間の宣言済み）∴ 触らない
