---
aim: 開発を駆動する目的を、人間だけが動かせる不動点としてリポジトリに保持し、エージェントは達成手段だけを担う
parent: bearing
state: open
---

# IS

**分割は所有で引く。** 目的（`aim:` 1 文）は人間のもので、エージェントは書き換えない。手段（body）はエージェントのもので、人間は妥当性を確認する。⚠ **この非対称が機構の全てである** —— 目的まで書けるエージェントは、達成できない目的を達成できる形に書き直せてしまい、そのとき失われたことは誰にも見えない。

**置き場はリポジトリである。** 目的は git artifact として commit され、vendor の memory にも会話にも住まない ∴ 機械が変わっても vendor が変わっても目的は残る。これは [[session-handoff]] が時間方向で担うことの、空間方向の対応物である。

**木で構造化する。** 1 目的 1 ファイル、親が目的、子が小目的。⚠ **body の中に階層を作り始めたら、それは子 aim が隠れている合図**であって、見出しを足す合図ではない。

⚠ **実装は前身から運ぶが、正当化は運ばない。** コードは動く実体として持ってくる。だがそれが *外付け拡張として* 妥当かは、ここで実際に使いながら書き直す。

**⚠ 置き場は repo だが、repo の中のどこかは project が決める（2026-09-03）。** 既定は `docs/aims/` のまま動かさない —— **既定が動けば、既に在る corpus が一斉に行方不明になる。** ⚠ **変えられるようにする理由は、[[bearing]] が全 project に載る形を採ったことである**: `docs/` が生成物である repo、package ごとに分ける monorepo、そもそも `docs/` を持たない repo に対して、**我々の doc 構成を採用の条件として押し付けない。**

⚠ **在り処の宣言は、採用の宣言と同じ 1 つの宣言である** —— [[bearing]] が `CLAUDE.md` へ置く marker が `dir=` を運ぶ。`dir=` が無ければ既定 ∴ **既に置かれた block は何もしなくてよい**（前方互換）。

⚠ **glob にはしない。3 つとも実測に基づく。** ⑴ **5 箇所が dir を git の pathspec として渡している** ∴ glob は pathspec magic の意味論に触れ、**走査が黙って広がりうる**。⑵ **slug が衝突する** —— `a/aims/x.md` と `b/aims/x.md` はどちらも slug `x` になり、`parent:` と `[[link]]` の解決が壊れる。**単一 root は木の同一性そのものに入っている。** ⑶ **「corpus が無い」と「別の場所を見ていた」が区別できなくなる** —— この repo が一貫して拒んできた沈黙の畳み方である。∴ **許すのは 1 つの明示的な dir だけ。**

⚠ **エージェントが「これを書いてよいか」と問うたなら、その問い自体が「書け」を意味する**（人間の決定 2026-09-08）。🔴 **`# IS` はエージェントの解釈を置く節である** ∴ **自力で導出できなかった解釈こそ、この節に欠けていたものである** —— **問うたという事実が、その不在の証拠である。**

⚠ **これは上段の非対称と矛盾しない。** 🔴 **問うべきは `aim:` を動かすときであって、`# IS` に解釈を足すときではない** —— **前者は人間の所有物に触る act、後者はエージェントの所有物を埋める act である。** ⚠ **2026-09-08、この repo のエージェントが後者で問うた** —— 導いた解釈を書いてよいかを尋ね、**人間は「聞いてくれたことそのものが、書くことを指示します」と述べた。**

⚠ **害は沈黙の側に在る。** 問いは往復を 1 つ増やすだけに見えるが、🔴 **問うて答えが来なければ、その解釈はどこにも残らない** —— **導出できなかったものは、次の世代も導出できない。** ⚠ **∴ 迷いは escalate の合図であると同時に、記録の合図でもある** —— **どちらを選ぶかは、触る先が誰の所有物かで決まる。**

⚠ **この規則は、この repo の corpus にだけ置く**（人間の決定 2026-09-08）—— **配る canon（aim skill の `aim-authoring.md`）へは昇格させない。** 🔴 **∴ 配る側で同じことを探しても無い** —— **無いのは漏れではなく決定である。**

# PROCESS

- [done] plugin の aim 機構（corpus の読み取り・fence 生成・記法検査）を移設した。5 枚の fence が実際に出ており、うち `drift-inter` は 2026-09-01 に実地で発火した
- [done] 目的を書くための canon（`_guide`）を、前身の場所前提を抜いた形で置いた（4 枚 ＋ README）。carrier への同梱は当時は生成であり、正本との同期を CI が検証していた（⚠ **2026-09-05 に `original/` が畳まれ、carrier 自身が正本になった** ∴ その対はもう無い）
- [done] **`# ESCALATION` を数え、frame と statusline に出した**（`escalation: N`）。⚠ **これは足りない機能ではなく、数が嘘をつく経路だった** —— 正本は「観測を可能にする作業は PROCESS、判断そのものは ESCALATION、そして観測と宣言は人間」と 3 つに分けているのに、**面を持つのは 2 つだけ**であり、`aim-facts.md` は「`open-todo` と観測待ちが両方 0 なら誰にも番が渡っていない」と**明示的に偽を述べていた**。⚠ **実測が支持した**: この corpus の `# ESCALATION` は 0/7 件、なのに判断待ちは実在し、**全部 baton にだけ在った**（`.handoff/` は gitignore ∴ 次に書けば archive される）—— **面に出ない節は書かれない。** ⚠ **見出しだけで中身の無い節は数に入れず、別の行で名指す**（`# PROCESS` の `unknown` と同じ法）。⚠ **色は `todo` / `観測待ち` と同じ blue にした** —— escalation が他より重いと面が述べれば、機械とエージェントに禁じている ranking を**色で**行うことになる
- [done] **section の走査を 1 つに畳んだ。** `# PROCESS` 用の scanner を複製して `# ESCALATION` 用を書けば、⚠ **「fenced block の中の見出しは引用であって主張ではない」という法が片方にだけ効く** —— そしてもう片方は、**この corpus 自身の doc が例として書いている `# ESCALATION` を実在として数える**。数えたことは誰にも告げられない
- [done] 🔴 **配られる法から「不可逆・破壊的な変更」の条項を外した**（人間の決定 2026-09-06）。**法は 6 条から 5 条になり**、外した条項は **`CLAUDE.md` の block の外**へ、この repo の規律として移した。

  🔴 **理由は文言ではなく射程である。** ⚠ **あの条項は aim 機構の語を 1 つも含まない** —— 履歴・force-push・データ・git だけを述べる。残る 5 条はいずれも `aim` skill・`aim:`/`parent:`/`state:`・body のマーク・`# PROCESS` を名指す。∴ **[[adoption-declaration]] の gate 越しに「aim の採用を宣言した repo」にだけ git の安全則が配られていた** —— ⑴ **配布が無関係な決定に紐づき**（採っていない repo は受け取らず、そちらのほうが corpus の規律も無い）⑵ **採用した側は頼んでいない射程を受け取っていた。** ⚠ **[[bearing]] の 3 柱のどれでもない** —— corpus 自身の会計でも、この block は `aim-tree` / `purpose-drift` の持ち物である。

  ⚠ **露見の経路を記す。** 空振りが 1 回起きて（merge 済み local branch の削除に escalate が要求され、何の判断も生まなかった）、エージェントは**文言だけを直した** —— 平坦な列挙を「不可逆性 × 沈黙」の原理へ寄せる形に。🔴 **置き場の問いを立てたのは人間である**（「frame にあったとのこと、aim 機構以外に触れているのか」）。⚠ **∴ より上位の問いが未解決のまま、下位の問いだけが解かれていた** —— **良い規則にしただけで、そこに在るべきかは問われていなかった。**

  ⚠ **移した先では原理を述べ直していない** —— `CLAUDE.md` の「無い」の節が既に「不可逆性 × 沈黙」を持つ ∴ **そこを指す。** **同じ原理を 2 度述べれば、片方だけが直る日が来る。**

  ⚠ **canon に触る ∴ 帰結は消費者へ及ぶ**: sha は `941df49f0cdaa3b2` → `6d8693bb3c91e20f`、版は **0.20.0**（minor。人間の決定）。bearing 自身の block は `bearing-setup-aim.mjs` が置き直した。⚠ **他の採用 repo は marker の版差から古さを読める** —— **これは marker が版と本文 sha を持つ理由そのものである。** ⚠ **ただし、あちらは条項が 1 つ*減った*ことを知らされない** —— 置いたものを plugin は追随させない（[[adoption-declaration]]）。

  ⚠ **再測は `sed -n '1p' CLAUDE.md` の marker と `templates/aim/frame.md` の条項数（5）を見るだけである。**
- [done] **frame の冒頭の断定を条件形にした。** 「このプロジェクトは aim で開発を進める」は corpus を持たない repo では偽であり、⚠ **frame は全セッションへ無条件に注入される** ∴ **その repo についての偽をエージェントに刻んでいた**（2026-09-02、他 repo で実測）。⚠ **偽になりうるのは冒頭 1 文だけだった** —— 残る 6 箇条は「aim に触れるなら」の条件文ゆえどこでも真である ∴ **frame 全体を corpus の有無で出し分けると、採用しようとしている新規 repo から法まで消える**。corpus が在るかは直後の facts が毎回述べる ∴ ここで断定する必要は最初から無かった。⚠ **2026-09-03、この判断に兄弟ができた** —— [[bearing]] が `CLAUDE.md` へ差し込む法の block は **opt-in した project にしか存在しない** ∴ **条件の意味があちらでは変わる** —— ⚠ **2026-09-03 の実装では条件を落とさなかった。****印は「採用した」ことであって「corpus が在る」ことではない** ∴ 採ったが node がまだ 0 の project では、あの条件は**今も真である**。⚠ **設計の段では「要らない」と書いた —— 実装で覆した。** 採用と corpus の存在を同一視していたのが誤りで、**その 2 つを分けることこそが印を入れた理由**だった。⚠ **同じ 6 箇条が 2 つの carrier に載り、片方だけが条件を持つ** —— これは食い違いではなく、**注入が無条件かどうかの差**である。⚠ **この理由を書かずに片方へ揃えると、opt-in の宣言を条件文で薄めるか、無条件の注入に偽を戻すかのどちらかになる**
- [done] **corpus の在り処を `resolveUnit` 1 箇所で解決し、そこから配る。** ⚠ **8 箇所の hardcode を「`CLAUDE.md` を読む」8 箇所の hardcode に置き換えてはならない** —— 読む場所が増えれば、どれが正かが再び分からなくなる。⚠ **実測した内訳**: `corpus.mjs` 2（slug の走査と slug→path）・`drift.mjs` / `unpushed.mjs`（既に定数）・`working-delta.mjs` 2 と `corpus-signature.mjs` と `corpus-delta.mjs`（git の pathspec）・`aim-facts.mjs`（canon の解決）。⚠ **解決の順序は「repo 自身の宣言 → unit root の宣言 → 既定」** —— repo 自身が先に来るのは、**corpus が repo の artifact だから**である（上の「置き場はリポジトリである」）∴ wrapper の宣言は、自分で名乗らない repo に対する既定にすぎない。⚠ **これは [[bearing]] に記録した既知の限界（印を unit root でしか読んでいない）を、副産物として解いた** —— 在り処を repo ごとに読むなら、印も repo ごとに読むことになる
- [done] **marker に `dir=` を足し、宣言された dir から法を組み立てる。** ⚠ **危ないのは sha の比較ではなく `desired` の作り方である** —— sha の比較は同じ file の中の 2 値で自己完結しているが、`desired` を既定 dir で組み立てると `inspect` は恒久的に `stale` と述べ、**CLI は custom dir の block を既定の本文へ黙って書き戻す**。⚠ **`dir=` だけが手で書き換えられた場合は、宣言を正として本文を追従させる** —— block の本文は我々のものであって人間の散文ではない ∴ 「人間の編集を消さない」には当たらない。⚠ **危ないと事前に述べた点が、実装で 2 度とも現実に出た** —— 渡し忘れの門が捕まえた: ⑴ `readOptIn` が既定 dir で法を組み立てており、**在り処を宣言した project が恒久的に「古い」と報告されていた**（人間は plugin を更新しても直らない理由を探すことになる）、⑵ `gatherBacklog` へdir を渡し忘れており、**`open-todo` が 0 と出ていた** —— **0 は「無い」に見えるので、誰も壊れたと気づかない**。⚠ **書き出しは `dir=` を省略しない**（読み取りは省略を許す）—— file を開いた人間が、どこを見ているかを知るために既定を覚えている必要は無い
- [done] **`frame.md` の `docs/aims/<slug>.md` を placeholder にする。** ⚠ **canon に触る** ∴ 生成される 3 つの carrier と、`CLAUDE.md` へ差し込む法の両方が追従する。本文が project ごとに変わるが、**sha は block ごとに採るので問題にならない**。⚠ **埋め残しは throw する** —— `{{aims}}` を残したまま配れば、**他人の repo のセッションが存在しない path を正本として読む**うえ、**placeholder は prose の中では意味ありげな記号にしか見えないので、誰も壊れたと気づかない**。⚠ **条件文は落とさなかった**（前段のとおり）
- [done] **「corpus 無し」を述べる全ての面に「どこを見たか」を持たせる。** ⚠ **不在と誤設定が同じ顔で出れば、`docs/aims/` を前提にしない repo で、設定の誤りが健康証明として読まれる** —— 面は statusline・frame・corpus fence の 3 つ。⚠ **statusline だけは既定のとき黙る** —— 既定を毎回描くのは、**この面が最も持たない予算（幅）を何も告げずに食う**ことである。⚠ **そして幅が確定しない文字を含む在り処は名指さない**（`別の在り処` と述べる）—— 名指せば画面が重なり、**述べようとした事実ごと読めなくなる**。⚠ **`normalizeAimsDir` は先頭の `/` を落とさず拒む**: 「repo root から」と「絶対 path」のどちらとも読める ∴ **曖昧なものを機械の側で決めない**（当初は落としていた —— 試験が曖昧さを暴いた）

- [done] 🔴 **`# OBSERVATION` を body の 6 番目の section として置き、面に出した。** ⚠ **これは 3 分割のうち面を持たなかった 1 つを埋めたのではない —— 埋めたのは*逆側*である。** 「観測と宣言は人間」には既に面が在り（宣言待ちの fence）、**節のほうが無かった** ∴ 面は「あなたの番だ」と述べながら、**何を見ればよいかを 1 文字も運んでいなかった。**

  🔴 **露見の経路を記す。** 全 11 node のうち 10 が宣言待ちに並び、**倒せば「aim に書かれた目的は全て達成された」と読める状態**になった（人間の問い 2026-09-06）。⚠ **各 node が自ら書いていた未観測の条件は、すべて散文の中にしか無かった** —— [[human-domain]] の「面は browser で 1 度も開いていない」、本 repo の親 node の「1 標本であって統計ではない」。**倒す前に確かめるべきものが、どの面にも出ていなかった。**

  ⚠ **`[todo]` にも `# ESCALATION` にも倒さないと決めた**（人間の決定 2026-09-06）。**todo に倒せば、法を破るか何も足さないかの二択になる** —— 「人間の返答を得る」は完了条件が人間ゆえ法に反し、「人間に問う」は**問うた瞬間に倒れる**（観測が 0 でも `[done]` になる）。**escalation に倒せば、あの数が「エージェントが止まっている数」でなくなる** —— 🔴 **escalation は「進めない」、宣言待ちは「終われない」**であり、前者は進行の停止、後者は完結の停止である。

  ⚠ **完了はエージェントが書かない。** 節は票だけを持ち、観測したという証言は frontmatter（`state:` ／ `last-verified:`）に置かれる ∴ **人間の宣言がエージェントを経由する経路を 1 本も増やしていない**（[[human-domain]] が畳もうとしている 3 手の形）。

  ⚠ **`last-verified:` は読む側にしか定義が無かった。** `checkpoint-stale` fence が読み、parser も test も在るのに、**書く側の canon が 1 度も述べておらず、この corpus の保有数は 0 だった**（実測 2026-09-06、`grep -l 'last-verified' docs/aims/*.md`）—— ⚠ **証言する場所だけが在っても、書けと述べる場所が無ければ使われない** ∴ canon の frontmatter の節へ足した。

  ⚠ **fence は改名した**（`bearing-awaiting-observation` → `bearing-awaiting-declaration`。field に `observations` を追加）—— **破壊的変更である。** 支えているのは [[adoption-declaration]] の「現時点の消費者は人間本人とその管理下の repo だけ」という**失効しうる前提**であり、**次に同種の変更を測る者は、まずその 1 行がまだ真かを確かめること。**

  ⚠ **版は 0.21.0**（minor。人間の決定 2026-09-06。前提「最重要の消費者は人間本人である」を再測したうえで）。🔴 **この bump では法の本文が 1 byte も動いていない** —— `frame.md` を触らないと決めたからである（節の定義は skill が持つ層であり、frame は既に「触れる前に skill を読め」と命じている）∴ **marker の sha は `6d8693bb3c91e20f` のまま、version だけが動いた** —— **この corpus で初めての形である。**

  ⚠ **再測は `node --test test/claude/bearing/process.test.mjs`（42 本）と、この repo の corpus に対する `observation: 10` ／ 宣言待ち fence の `observations` 列である**（実測 2026-09-06）

# ESCALATION

🔴 **`last-verified:` をどこへ置くか —— あるいは置かないか。**

⚠ **2026-09-10、人間が frontmatter の射程を述べた**（人間の決定 2026-09-10）—— 🔴 **「私が frontmatter に求めるのは『目的の一文・親を示す・満足と諦めの状態』のみ」** ∴ **`aim:` / `parent:` / `state:` の 3 つであり、`last-verified:` は frontmatter に属さない。**

⚠ **だが行き先が決まらない。** 🔴 **body へ移せば「人間が body に書く」ことになり、「frontmatter は人間のもの・body はあなたのもの」という分割そのものに触れる** —— **これはこの node の中心である。** ⚠ **併せて、`last-verified:` は同じ欠陥を持つ** —— **人間が sha を書く欄であり、host の merge がそれを書き換える**（[[purpose-drift]] が照合について直したのと同型。**しかもこちらは人間のものであり、黙って無効になる**）。⚠ **保有数は 0 である**（実測 2026-09-06、対象: この repo の corpus）∴ **まだ誰も踏んでいない。**

**候補**: ⑴ **`# OBSERVATION` へ人間が 1 行書く形にする**（分割を緩める）⑵ **`checkpoint-stale` ごと廃する**（保有数 0 ＋ `state:` が満足を既に運ぶ）⑶ **frontmatter に残す**（3 つに絞るという決定を、この 1 field だけ例外にする）。⚠ **⑴⑵ はどちらも配る法の改訂を含む。選ぶのは人間である。**

# OBSERVATION

- **`open-todo` / `escalation` / `observation` / 宣言待ちの 4 つが、実際に注意を向けるべき node へ導いたか。** 1 サイクル使ったうえで —— ⚠ **数が読まれずに素通りされたなら、それは面が在ることの反証である。**
- **配られる法が 5 条であることを、採用側の `CLAUDE.md` の block で見る**（2026-09-06 に 6 条から減らした）。⚠ **消費者は marker の版差から「古い」しか読めない** ∴ **何が減ったかは人間が現物を見るしかない。**
- **frontmatter が実際に人間の手でしか動いていないか。** ⚠ **今この境界を守っているのは、エージェントが指示に従っているという事実だけである** —— `git log -p -- docs/aims/` で `aim:` / `parent:` / `state:` の変更が誰の act だったかを人間が確かめる。
