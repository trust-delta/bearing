---
aim: 目的と、それを実装した手段や隣接する目的とのズレを、安く機械的に可視化する。判定はしない
parent: bearing
state: open
---

# IS

**層が 3 つあり、機械が担うのは第 1 層だけである。** 安い機械検知＝**可視化**、エージェント＝検査と自明な保守、人間＝判断。⚠ **`aim:` が「判定はしない」と明記しているのはこのためで**、可視化が判定に滑ると、目的が満たされたかの宣言を機械が肩代わりすることになる。

**ズレは 2 種ある。** 同じ目的の中でのズレ（body が自らの `aim:` から離れる）と、目的どうしのズレ（隣接が互いを反映していない）。⚠ **trigger が違うので機構も分かれる** —— 前者は anchor の*変更*のみ、後者は*作成*も trigger になる（親を動かさず子を足す形があるため）。

⚠ **悪いセンサーは無いセンサーに劣る。** 数だけは権威に見えるので、黙って数え落とすことも、観測できないものを 0 に倒すことも、沈黙より悪い。**観測できなかったときは、そう言う。**

⚠ **そして本 node は当面ほとんど沈黙する** —— 履歴を読む機構なのに、この corpus に履歴がまだ無いからだ（[[bearing]] の IS 参照）。沈黙が「ズレが無い」を意味しないことを、この時期は特に守る必要がある。

⚠ **この機構が見るのは、git 履歴の中の `docs/aims/` だけである。** ∴ **構造的にここへ映らない drift がある** —— README の日本語正本と英語版の食い違い、散文と code の言語の混在（[[operator-language]]）。それらには別のセンサーが要り、実際に `scripts/lang-report.mjs` が置かれて CI で毎回走る。⚠ **あちらも同じ法に従う**（可視化するだけで判定しない・落とさない）が、**ここが黙っていることは、あちらが黙っていることを意味しない。** 沈黙の範囲を取り違えないこと。

# PROCESS

- [done] plugin の drift 機構（intra / inter の 2 fence）を移設した
- [todo] **履歴の浅い corpus で、沈黙が「健全」と読まれないようにする。** 現在の fence は「ズレ無し」と「履歴が浅くて言えない」を同じ空欄で出す
