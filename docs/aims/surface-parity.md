---
aim: 同一エージェントの起動経路が複数ある場合、そのUI上の表示を可能な限り揃える
parent: bearing
state: open
---

# IS

**同じ設定を読み、同じ hook を走らせ、同じ skill を持つのに、UI だけが違う。** Claude Code は CLI・デスクトップの Code・web から起動でき、docs が明言する通り `~/.claude/settings.json` も hook も skill も**共有される** —— にもかかわらず、何が画面に出るかは経路ごとに違う。⚠ **∴ 人間は経路を変えるたびに「今どれが見えていないか」を覚え直すことになり、その負担は道具ではなく人間が払っている。**

**⚠「可能な限り」は制約の受容ではなく、この node の仕事そのものである。** 揃えられないものは実在する —— statusline はデスクトップに存在せず、usage ring は CLI に存在しない。**ハーネスが提供していない面は、外付けの拡張には作れない。** ∴ ここでやるべきは妥協の表明ではなく、**どこまでが可能かを実際に測り、可能な範囲を埋め切ること**である。測っていない「可能な限り」は、単に諦めの別名になる。

⚠ **本 node も柱ではない**（人間が 2026-09-02 に判定）。経路ごとに UI が食い違うことは [[bearing]] の 3 点のどれでもなく、**3 点をスムーズに遂行させることを妨げる摩擦**である —— 同じ事実が片方の経路でだけ見えないなら、人間はその経路にいる間だけ目的を見失う。∴ ここで揃えるのは*表示*だが、守っているのは**どの経路から入っても同じ木を見ていられること**である。

**2026-09-01 に測った非対称は 4 つだった。**

| 事実 | CLI | デスクトップの Code |
| :-- | :-- | :-- |
| context / plan usage | 無い → [[ambient-display]] が描く | **usage ring**（ハーネスが持つ） |
| PR の review state | footer badge | footer badge（**揃っている**） |
| CI の通過 / 失敗 | **無い** | **CI status bar**（auto-fix / auto-merge 付き） |
| aim / baton / drift | [[ambient-display]] の 2 行目 | **無い**（statusline が存在しない） |

⚠ **埋め方は面ごとに違う。** CLI 側の穴は statusline で埋まる（token を食わない）。**デスクトップ側の穴には対応する面が無い** ∴ hook の会話注入か、人間が呼ぶ on-demand の手段に頼るほかなく、**どちらも token を食う** —— つまり**同じものを見るための費用が経路ごとに違う**。⚠ この非対称は揃えられない。揃えられるのは*見えるかどうか*であって*費用*ではない。

⚠ **2026-09-03、5 つ目の非対称が決着として確定した。** 「bearing が載っていない」は CLI では [[ambient-display]] の shim が描くが、**デスクトップには描く面が無い** —— [[bearing]] の人間が「plugin 側は自分の実体を検査しない」を採った ∴ **hook で埋める道も選ばれていない**。⚠ **これは測らずに言う「可能な限り」ではない** —— 公式 marketplace の 54 plugin を測り、この層を追う plugin が 0 件であることを確かめたうえで、負うと決めた非対称である。

🔴 **2026-09-07、人間が「可能な限り」の上界を確定させた**（人間の決定 2026-09-07）—— **「CLI 側の不足は statusline と command（script）で補い、デスクトップ側の不足は会話へ載る形で補う。そこで補いきれるかは別問題であり、そもそも 1mm も補えないこともある」。** ∴ **上界を引くのは目的の側の裁量ではなく、経路ごとに実在する器である** —— CLI は statusline と人間が打つ command、デスクトップは会話への注入。⚠ **その器で届かないところは「揃えないと決めた面」ではなく、構造的に届かない面である** ∴ **「揃えないと決めた面」として実在するのは、上段の 1 件（2026-09-03）だけである。** ⚠ **補いの十分さ —— 鮮度・費用・手数 —— は、この上界の問いではない**（人間の言葉で「補いきれるかは別問題」）∴ **それは手段の側にあり、`# PROCESS` が持つ。** ⚠ **人間の言葉は「デスクトップ側は会話(hook)」だったが、実際に埋めた 1 件は hook ではなく `/bearing:facts`（人間が呼ぶ on-demand の command）である** ∴ ここでは「会話へ載る形」と記した —— **注入の担い手は 2 通りある。**

# PROCESS

- [done] **2 つの経路が持つ UI を実測し、非対称を上の表にした。** 出典は docs（statusline / interactive-mode / desktop）と実際の画面である。⚠ **記録は上の表そのものであって、別の file ではない** —— 実測の作業ログは machine-local に在るが repo には載らない（#6）∴ ここから名指すと、clone した者には辿れない参照になる
- [done] **CLI 側に欠けていた context / plan usage / branch を埋めた** —— 手段は [[ambient-display]] が持つ
- [done] **同じ事実を運ぶ 2 つの面が、同じ述語で黙るようにした。** hook は `CLAUDE.md` の marker を見て黙るのに、statusline は corpus の有無しか見ておらず、**aim を採っていない全 project に 2 行目を描いていた**。⚠ **何を基準に揃えるかは [[adoption-declaration]] が持ち、本 node はどこが揃っていないかを述べる。** ⚠ **これは「どの面に何が出るか」の非対称ではなく、*同じ面同士の食い違い*である** —— この node が測ってきた欠落とは別の種であり、**user スコープで載せるまで見えなかった**（採った repo でしか面を見ていなかった）。手段は [[ambient-display]] が持つ
- [done] **デスクトップ側から aim / baton / drift へ届く経路を用意した** —— `/bearing:facts`。⚠ **経路そのものは既に在った**（`aim-facts.mjs` は plugin の `bin/` に在り、Bash tool の PATH から裸で呼べる）∴ **欠けていたのは道ではなく、呼べる形である。** ⚠ **出るものは SessionStart hook と同じで、別の記述を持たない** —— 儀式についての第 2 の記述を作れば、この repo が一貫して禁じてきた形になる。⚠ **これは token を食う** —— 本 node が上で述べたとおり、**デスクトップ側の穴には対応する面が無く、会話注入か on-demand に頼るほかない。費用の非対称は揃えられない。揃えられるのは*見えるかどうか*である。**⚠ **statusline の 2 行目と同じ事実が出るが、同じ*見え方*にはならない** —— あちらは黙って在り続け、こちらは 1 度だけ喋る
- [done] **CI の通過 / 失敗を CLI 側でも見られるようにした。** 🔴 **採る側と読む側を分けた** —— 採るのは `bin/bearing-ci.mjs`（`/bearing:ci`）で、`~/.bearing/units/<unit>/ci.json` へ**採った時刻つき**で置く。**面は置かれた JSON を読むだけ**であり、⚠ **statusline は決して `gh` を起こさない**（あの面は assistant message ごとに走り debounce で殺される ∴ 起こした子は宙に浮き rate limit も踏む）。✅ **その分離を test が固定する** —— statusline の source が `probeCi` / `writeCi` を含まないことを検査する。⚠ **CI は 1 行目に置いた** —— aim と無関係な事実であり、[[adoption-declaration]] の gate で黙らせれば採っていない repo が CI を見失う（**baton と同じ例外**）。⚠ **古い緑は緑ではない** ∴ 採ってから 30 分を過ぎれば結論ではなく `CI 古い` と述べ、時刻が読めないものも同じ側へ倒す。⚠ **run が 0 本なのは「まだ走っていない」であって「緑」ではない**（`CI 無し`）。⚠ **`gh` の不在・認証の不在・時間切れを「CI が無い」に畳まない**（`CI 不明` ＋ 理由を JSON に残す）。⚠ **branch が違う結果は描かない** —— 他の branch の緑を今の緑として見せることは、この機構が最も避けたい形である。🔴 **`consumer-check` が退行を 1 件捕まえた**（2026-09-06）—— **新しい `lib/ci.mjs` を untracked のまま置いたら、****出荷 copy は tracked file だけを写すので module が欠け、statusline は在らないものを import して即死し、面から 2 行が黙って消えた。** ⚠ **これは [[ambient-display]] が warn している壊れ方そのものであり、`node --test` は 391 本すべて緑のままだった** —— **捕まえたのは消費者を合成する検査だけである。** ⚠ **再測は `node scripts/consumer-check.mjs`（26 件）である**
- [done] **`/bearing:ci` が「間近に走った commit の run を*全て*」見るようにした。** 🔴 **旧い `--limit 1` は、1 度の push で走る複数 workflow のうち 1 本しか読まない** ∴ **面は「1 本の緑」を「全体の緑」として描きえた。** ⚠ **仮定ではなく踏んだ形である**（実測 2026-09-07、対象: この機体の `gh`）—— `44ff61ef` の `push-policy` が completed/success の時点で `ci` はまだ `in_progress` であり、**`--limit 1` は前者を返した**（採ったのは `ci` が終わった後で、置かれた JSON は `push-policy` を名乗っていた）。∴ **`gh run list --limit 20` を打ち、先頭 run の `headSha` と同じ commit の run だけを最悪値へ畳む** —— 1 本でも走っていれば `CI 実行中`、1 本でも通っていなければその 1 本を名指す。⚠ **`skipped` は失敗ではない**（条件で走らなかった job）∴ 通過側へ数え、それ以外は `neutral` でも名指す。⚠ **前の commit の run は混ぜない** —— 混ぜれば畳んだ結論が嘘になる。⚠ **上限に当たったまま全部が同じ commit なら「取りこぼしを排除できない」と述べて `CI 不明` へ倒す** ——「見えた範囲では全部緑」を「全部緑」に畳まない。🔴 **内訳（`workflows`）と `headSha` を JSON と stdout に残した** —— **面に置けるのは 1 語だが、この command の肝は「人間が GitHub の web も `gh` も打たずに、CI の結果をエージェントと共有できること」である**（人間の決定 2026-09-07）∴ **何本見たかが要る。** ⚠ **`headSha` は記録であって判定には使わない** —— 今の HEAD と照合する形は**未 push の commit を抱えた間ずっと「run が無い」になる**別の設計であり、人間は「この command は CI が走っていることを前提に打たれる ∴ 見るべきは*間近に走った CI が全て通ったか*である」と定めた（人間の決定 2026-09-07）。⚠ **再測は `node --test test/claude/bearing/ci.test.mjs`（14 本）である**
- [done] **`/bearing:ci` の照合先を、HEAD でも「先頭 run」でもなく `@{u}`（push 済みの commit）にした。** 🔴 **旧い形は、今の commit の run がまだ登録されていない窓で、1 つ前の commit の結論をそのまま描いた** —— ⚠ **仮定ではなく踏んだ形である**（実測 2026-09-07、対象: この機体の `gh`。**merge 直後に 2 回とも踏んだ**: `9b7f576` と `66d378f` で、いずれも 1 回目の probe が 1 つ前の commit の 2 本を読んで「緑」と述べた）。⚠ **緑とは限らない** —— **前の commit が赤なら赤が持ち越される** ∴ **非対称ではなく commit の取り違えである。** ⚠ **面に置けるのは 1 語で、そこに commit は入らない** —— **読み手には区別できなかった。**

  🔴 **却下されていた「HEAD と照合する」案の理由は正しかった** —— **未 push の commit を抱えた間ずっと「run が無い」になる**（人間の決定 2026-09-07）。⚠ **誤っていたのは照合先のほうである**: **CI が走るのは push された commit であって、手元の HEAD ではない** ∴ **`@{u}` と照合すれば、あの理由は発生しない。** 🔴 **人間がこの節を開き直した**（人間の決定 2026-09-07、「根治の手があるならそちらに切り替えましょう」）。

  ∴ **`@{u}` が指す commit の run だけを畳む。** その commit の run が **0 本なら `CI 無し`**（**前の結論は決して持ち越さない**）。⚠ **上限に当たっていて見えないだけの場合と区別する** —— 20 本すべてが別 commit なら「取りこぼしを排除できない」と述べて `CI 不明` へ倒す。⚠ **upstream が無い branch は「まだ push されていない」であって「採れなかった」ではない**（`note` に置き、面は `CI 無し`）。🔴 **HEAD が `@{u}` の先に居るときは、面がそう言う**（`CI 通過(未 push 2)`）—— **さもなくば読み手はこの緑を*手元の変更*の緑として読み、直した意味が半分になる。**

  ⚠ **`@{u}` は「我々が push した先」の記憶である** —— **他者が push した分は `git fetch` するまで見えない**（採取のたびに repo の ref は書き換えない）。⚠ **塞げていない窓が 1 つ残る**: **その commit で何本走るはずかを我々は知らない**（workflow は条件で走らないことがあり、静的には決まらない）∴ **2 本走るはずが 1 本しか登録されていない瞬間に、その 1 本が緑なら「通過」を描きうる。** ⚠ **これは code の読みであって実測ではない** —— 2026-09-07 に観測したのは、いずれも 0 本登録の側である。

  ✅ **変異試験で確かめた**（実測 2026-09-07）—— 照合先を旧い「先頭 run の commit」へ戻し、未 push の注記を落とすと **19 本中 3 本が赤**、復元で 19 本緑。

  🔴 **同日、実地で採ったら足りていなかった —— `--branch` で引いて手元で絞る形は、main では常に `CI 不明` へ倒れた。** 実測 2026-09-07、merge 直後の `dbff7598`: **run が溜まった branch では `--limit 20` の窓が常に埋まっており、「まだ走っていない」と「窓の外」を区別できない** ∴ **安全側に倒すと、push 直後は必ず「取りこぼしを排除できない」になる。** ∴ **`gh run list --commit <@{u}> --limit 20` で、その commit の run を直接引く** —— **0 本は本当に 0 本であり、窓の外という疑いが要らない。**

  ⚠ **その過程で地雷を 1 つ掘り当てた**: 🔴 **`--commit` に短縮 sha を渡すと、在る run を黙って 0 件と返す**（実測 2026-09-07、対象: この機体の `gh` 2.85.0 —— `66d378f3…` の full sha では 2 本返り、先頭 8 桁では 0 件）∴ **切り詰めれば「無い」が嘘になる。** ⚠ **test は「渡す sha を切り詰めない・`--branch` で引かない」を固定する** —— **挙動ではなく、二度と入れさせない形を固定している。** ⚠ **再測は `gh run list --commit <full> --json headSha` と、同じ sha の先頭 8 桁である。**

  ⚠ **実地で見た 3 つを並べる**（いずれも 2026-09-07、対象: この機体の `gh`）—— **旧々（先頭 run 照合）は merge 直後に `CI 通過` と描いた**（前の commit の緑。2 回）／ **`--branch` 引きは `CI 不明` と述べた**（`dbff7598`）／ 🔴 **`commit` 直引きが `CI 無し` を述べるところは、まだ実地で見ていない** —— **同時刻に `gh run list --commit dbff7598` が `[]` を返したことは確かめたが、それは道具の側の観測であって、この command の出力ではない。** **再測は、次に main へ push した直後の `/bearing:ci` である。**

  ✅ **その再測を実際に行った —— 2 つとも取れた**（実測 2026-09-07、対象: この機体の `gh`、`0a85b69d` の merge 直後）。⚠ **採り方は「速く 1 回」ではなく「3 秒間隔で連続」である** —— **前の 4 世代はいずれも 2 コマンドの間に窓が閉じて逃していた。**

  ⑴ 🔴 **commit 直引きの弁別点**: **1 つ前の commit（`dbff7598`）の 2 本が緑で在るのに、`状態: none`「push された commit の run がまだ 1 本も無い」と述べた。** ⚠ **同じ瞬間、旧々（先頭 run 照合）なら `CI 通過`、`--branch` 引きなら `CI 不明` である** ∴ **3 つの形が実地で別々の答えを出すことを、同じ窓の上で確かめた。**

  ⑵ 🔴 **「1 本でも実行中なら実行中へ倒す」の実地確認**（実測 2026-09-07、対象: この機体の `gh`）—— **`push-policy` が completed/success、`ci` が in_progress の状態を実際に見た。** 面は `CI 実行中`、名指しは `ci`。⚠ **これは 4 世代にわたって取り逃していた観測であり、それまで固定していたのは単体テストだけだった。**

  ── **限界**（人間の依頼により明示。2026-09-07）──

  🔴 **⑴ その commit で何本走るはずかを、我々は知らない。** `gh run list` は**起動された run しか知らず、起動されるはずだった run を知らない** ∴ **「登録済みが全部緑」を「その push の CI が全部緑」に畳めない。** ⚠ **今回、登録は 2 本まとめて現れた**（0 本 → 2 本。実測 2026-09-07、対象: この機体の `gh`、1 回のみ）—— **「1 本だけ登録されている瞬間」は観測していない** ∴ **その窓が実在するかは未確認である。** ⚠ **無いことの証拠ではない。**

  🔴 **⑵ その外側の層を測った —— 狭まらなかった**（人間の依頼により実施。実測 2026-09-07、対象: GitHub REST（`gh` 2.85.0）、`0a85b69d`）。**3 つとも「走るはず」を教えない:**

  | 層 | 返ってきたもの | なぜ使えないか |
  | :-- | :-- | :-- |
  | `commits/<sha>/check-suites` | **5 suite**: `github-actions` 2 つ（completed/success、check run 5 本と 1 本 ＝ job 数）と、**`firebase-app-hosting` / `vercel` / `cloudflare-workers-and-pages` が `queued`・0 runs** | 🔴 **走らない外部アプリの suite が `queued` のまま残る** ∴ **素朴に「全部 completed か」を見れば、緑の commit が永遠に実行中になる。** `github-actions` だけに絞れば、**今の層と同じ情報に戻る** |
  | `commits/<sha>/status` | `state: pending`、contexts **0 件** | ⚠ **Actions は check run を使い commit status を使わない** ∴ **`pending` を信じれば、緑の commit を保留と読む** |
  | `branches/main/protection` | **404「Branch not protected」** | **required checks の宣言がそもそも無い** ∴ 「走るはず」が定義されていない |

  🔴 **∴ 測ったことで、採らずに済んだ** —— **check suite を素朴に見に行っていたら、外部アプリの `queued` という新しい嘘を 1 つ抱えていた。** ⚠ **一般には required checks を宣言した repo なら「はず」は在りうる** —— **この repo は宣言していない**し、**宣言しても path filter や `if:` で走らない workflow は別問題である。** ⚠ **再測は上の 3 本の `gh api` である。**

  🔴 **⑶ PR の面が見ているものを、我々は全部は見ていない**（実測 2026-09-07、対象: GitHub REST/GraphQL、`3caa8712` ＝ PR #47 の head）。**`gh pr checks 47` は 6 件返すが、`gh run list --commit` は 1 件しか返さない。** 差は層である:

  | 層 | 見えるもの |
  | :-- | :-- |
  | `gh run list --commit`（**今の実装**） | **workflow run 1 件**（`ci`） |
  | `commits/<sha>/check-runs` | **check run 5 件**（`ci` の job 群。⚠ CodeRabbit は居ない） |
  | `commits/<sha>/status` | 🔴 **`CodeRabbit` 1 件** —— **legacy の commit status** |
  | `gh pr checks` / `statusCheckRollup` | **6 件** ＝ 上 2 つの**和** |

  ∴ ⚠ **負った穴は 2 つ**: ⑴ **commit status を 1 つも見ていない** ∴ **PR の branch で `CodeRabbit` が赤でも `CI 通過` と描く**（⚠ **main では statuses が 0 件** —— 実測 2026-09-07、`0a85b69d` ∴ **実害は PR branch 上に限られる**）。⑵ **粒度は workflow であって job ではない** —— `ci` の 5 job は 1 本に畳まれる。

  🔴 **人間はこれを承知のうえで、暫定で今の層に留まると決めた**（人間の決定 2026-09-07）——「**より正確で穴のない手段、または齟齬の少ない方法が見えるまで**」。⚠ **∴ これは「見ていない穴」ではなく「負った穴」である** —— **見えていて、記録されていて、条件つきで留保されている。**

  🔴 **同日、その留保は解けた —— 人間が「主眼は揃えることである」と述べ、揃える手を採った**（人間の決定 2026-09-07）。⚠ **解いたのは新しい思いつきではなく、あちらが何を見ているかを測ったことである。**

  ⚠ **測り方は、同梱バイナリの文字列である**（実測 2026-09-07、対象: この機体の Claude Code 2.1.263 の ELF）—— `commits(last:1){nodes{commit{statusCheckRollup{` と `contexts(first:0){checkRunCountsByState{state count} statusContextCountsByState{state count}}` が在った。🔴 **∴ あちらは「PR の statusCheckRollup」を、個々の check を 1 件も取らずに、状態ごとの件数だけで見ている。** ⚠ **これは文字列からの推定であって、あちらの実装の記述ではない** —— **再測は同じ binary への `grep -a` である。**

  🔴 **∴ 揃え方は「同じ集合を見ること」ではなく「同じ判定に至ること」である** —— **判定は `statusCheckRollup.state` に委ねる。** ⚠ **自分で畳めば、`NEUTRAL` や `SKIPPED` の扱いが 1 つ違うだけで、同じ画面を見ている 2 つの経路が別のことを言う。** ⚠ **内訳だけは `contexts(first:100)` で取る**（あちらは件数しか取らない）—— **面に置けるのは 1 語だが、この command の肝は人間とエージェントが内訳を共有できることである。**

  ⚠ **これで、上に書いた「負った穴」の ⑴ は閉じた** —— **status context（`CodeRabbit`）が rollup に含まれる** ∴ **PR の branch で赤を見落とさない。** ⚠ **⑵ の粒度も job へ揃った。** 🔴 **そして GitHub にしか無い状態が 1 つ手に入った**: **`EXPECTED`（報告されるはずだが未着）** —— **`gh run list` の層では決して知りえなかったものである。**

  ⚠ **揃えられないところは残る。** 🔴 **あちらの面は PR 文脈にしか存在しない** —— **main への直 push には PR が無い** ∴ **そこは我々の層（`gh run list --commit <@{u}>`）が答え続ける。** ⚠ **これは 2 つの実装ではなく 2 つの*問い*である**: **同じ問いには同じ答えを返し、あちらが問いを持たない場所では我々が独自に答える。**

  ✅ **変異試験で確かめた**（実測 2026-09-07）—— **StatusContext を内訳から落とし、知らない rollup state を緑へ畳むと 25 本中 3 本が赤**、復元で 25 本緑。⚠ **再測は `node --test test/claude/bearing/ci.test.mjs`（25 本）である。**

  ⚠ **切り替え先は測って分かっている** —— **`commits/<sha>/check-runs` ＋ `commits/<sha>/status` の 2 本**で、PR の面と同じ集合が得られる。🔴 **PR 番号は要らない**（commit で引ける）。⚠ **罠が 1 つ**: **statuses は contexts が 0 件のとき `state: pending` を名乗る** ∴ **combined state を信じてはならず、contexts を数えること。** ⚠ **どちらへ移っても「走るはず」は分からない** —— 見えるのは **作られた check** だけである。

  ⚠ **⑷ `@{u}` は「我々が push した先」の記憶である** —— **他者が push した分は `git fetch` するまで見えない**（採取のたびに repo の ref は書き換えない）。

  ⚠ **⑸ `CI 古い`（30 分）は時間の話であって commit の話ではない** —— **採取が新しいことは、それが今の commit についてであることを意味しない。** ⑴〜⑷ を塞いだのはそちらの軸である。

  🔴 **人間はこの限界をこう述べた**（人間の指摘 2026-09-07）—— **「走るはずと知って計測する必要がある。機械的な判別だけでなく、人間やエージェントの判断あってこそ」。** ⚠ **この node はそれを、測ったうえで受け取る** —— **⑵ を測った結果、外側の層にも「走るはず」を教える面は無かった**（この repo では）。⚠ **それでも「原理的にそうだ」とは書かない** —— **測ったのは GitHub の 3 つの層であって、あらゆる層ではない。** ⚠ **そして骨は、この repo が既に持っている法と同じである**: **面が述べられるのは「見えた範囲」であって、「全部か」の宣言は別の層に属する。**⚠ **再測は `node --test test/claude/bearing/ci.test.mjs`（19 本）と、main へ push した直後の `/bearing:ci`（`- 照合した commit (@{u}):` が今の HEAD を名乗り、run が無ければ `CI 無し` と述べること）である**


# OBSERVATION

- **2 つの起動経路を実際に並べ、揃っていないと感じる面が残っていないか。** ⚠ **表は 2026-09-01 の実測であり、向こうが変えれば黙って偽になる** —— **我々の機構はそれを検知しない。**
- **`/bearing:facts` がデスクトップ側で実用になったか。** ⚠ **道が在ることと、使われることは別である** —— 呼ばれなければ、埋めた非対称は紙の上でしか埋まっていない。
- 🔴 **デスクトップの Code の CI status bar と、CLI の面に出る 1 語（`CI 通過` 等）を、同じ commit について並べて読み、両者が同じことを述べるか。** ⚠ **揃えたのは*見ている集合*ではなく*判定*である** —— `statusCheckRollup.state` を GitHub に畳ませ、我々は畳まない（`# PROCESS`、人間の決定 2026-09-07）。✅ **変異試験と単体テストが固定するのは、我々がその state をどう写すかだけである** ∴ ⚠ **あちらが同じ state をどう*描く*かは向こうの画面にしかない。** 🔴 **そもそも「あちらが rollup を見ている」こと自体、同梱バイナリの文字列からの推定である**（実測 2026-09-07、対象: この機体の Claude Code 2.1.263 の ELF）—— **並べて食い違えば、偽なのは実装ではなく推定のほうである。**
- **`/bearing:ci` を打ったあと、CI の結果を知るために GitHub の web も `gh` も開かずに済んだか。** ⚠ **肝は面が 1 語を描いたことではなく、「人間が GitHub の web も `gh` も打たずに、CI の結果をエージェントと共有できること」だと人間が定めた**（人間の決定 2026-09-07）∴ **測るべきは、別の窓を開く手が止まったかである。** ⚠ **上の `/bearing:facts` の票と同じ法だが、別の面についての別の票である** —— **道が在ることと使われることは、面ごとに別々に決まる。**
- ⚠ **面の 1 語が、文脈によって別の層から来ていることに読み手が躓かなかったか。** 🔴 **PR が在れば `statusCheckRollup`、無ければ `gh run list --commit <@{u}>` から来る**（`lib/ci.mjs` の `probeCi`）—— **同じ `CI 通過` でも見ている集合が違い、面に出る 1 語はそれを運ばない。** ⚠ **これは 2 つの実装ではなく 2 つの問いである**（`# PROCESS`）∴ **設計としては正しいが、正しさは読み手の体験ではない。** ⚠ **上の「CI status bar と並べて読む」票とは軸が違う** —— あちらの非対称は*経路の間*にあり、こちらは**同じ CLI の中で、PR branch と main を往復する間**にある。
- 🔴 **面が `CI 通過` と述べた直後に赤が現れる —— その瞬間を踏まなかったか。** ⚠ **我々は「その commit で何本走るはずか」を知らない**（`# PROCESS` の限界 ⑴。外側の 3 層を測っても教えなかった —— 実測 2026-09-07、対象: GitHub REST）∴ **登録が揃う前に畳めば、面は偽の緑を描く。** ⚠ **その窓が実在するかは未確認である**（実測 2026-09-07、登録は 0 本 → 2 本とまとめて現れた。1 標本）—— **踏まなかったことは、無いことの証拠ではない。** 🔴 **偽の緑は、揃っていないことより悪い** —— **揃っていない面は人間に別の窓を開かせるが、偽の緑は開かせない。**

# DAG

- 依存: [[ambient-display]] —— CLI 側を埋める手段はあちらが持つ。本 node が述べるのは*どこが揃っていないか*であって、埋め方ではない
