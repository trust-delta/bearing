---
aim: 標準的なコーディングハーネスに欠けている「目的の保持・文脈の引き継ぎ・ズレの検知」を、ハーネスを置き換えずに外付けの拡張として供給する
state: open
---

# IS

**この repo が想定するのは個人開発である。** 1 人の人間が 1 つのセッションと対話しながら進める形 —— いわゆる vibe coding —— が前提であり、⚠ **それは規模の遠慮ではなく、機構が成り立つ条件である。** 「目的は人間のもの」「迷ったら人間に escalate する」「達成の宣言は人間が行う」は、**その人間が誰かを問う必要がない**からこそ 1 文で書ける。⚠ **複数の人間が関わる project では、この体制は『誰に escalate するのか』を別途決めない限り成立しない** —— それは道具の設定ではなく、その project が自分で書くべき前提である。

**器は与えられている。** 本 repo は場を作らない —— agent が動く場所は標準ハーネスが既に十分に供給しており、それを wrap する仕事はもう空いていない。∴ 手段は「**外付けの拡張**」に限られ、現時点でそれは Claude Code の plugin である。⚠ **plugin は手段であって目的ではない。** `aim:` が「拡張機構」でなく「外付けの拡張として供給する」と述べているのは、器が変われば形も変わるからだ。

**供給の形は 3 つあり、選択は「宣言をどこに置くか」に尽きる。** 本 repo が採っているのは project の `.claude/settings.json` に `extraKnownMarketplaces` ＋ `enabledPlugins` を書く形で、⚠ **tracked ゆえ clone した者にも同じものが載り、載っている事実自体が git に残る。** 他に `~/.claude/settings.json` へ書く形と、user scope で `claude plugin install` を打つ形があり、この 2 つは cwd に依らず**全 project**に効く。⚠ **だがどちらも untracked である ∴ 載せたという記録がどこにも残らない** —— 前身が `~/.claude/skills/aim` の symlink で全 dir に載り、やがて所在が分からなくなったのと**同じ層の同じ罠**であり、`bearing-trace` にも原理的に映らない。∴ **「全 project に効く」と「載っていることが後から辿れる」は、この層では両立しない。** ⚠ どちらを取るかは、機構の性能ではなく人間が何を失う覚悟をするかの問いである。 ⚠ **2026-09-02、この二律は仮定でなくなった** —— 同日 user scope の install が実際に起き、**tracked な宣言と untracked な entry が同時に効いている**。踏んだ記録は `~/.claude/plugins/installed_plugins.json` にしか無く、**本 repo からは辿れない。**

**⚠ 同日、人間が project スコープを選び、user スコープを外した。** 理由は 2 つある —— ⑴ **user スコープは「より汎用的で普遍なもの」の置き場**であり、bearing は `docs/aims/` という **doc 構成を前提にする** ∴ そこへ置く性質のものではない。⑵ **他人の repo へ貢献するとき、明らかに邪魔になる。** ⚠ **そして記憶で答えられなくなる事態は、選択より先に既に起きていた** —— 人間は user スコープに入れた覚えが無く、実際には入っていた。上が述べた untracked の罠は、**予測ではなく観測になった。**

⚠ **だが「邪魔さ」を実測すると 1 箇所に集中していた。** PreCompact は corpus と `.handoff/` の不在を見て既に自ら降りており、UserPromptSubmit も PostToolBatch も黙る。⚠ **残っていたのは SessionStart だけで、しかも害は量ではなく偽だった** —— frame の冒頭が無条件に断定していた。∴ **スコープの選択とは別に、frame 自身の欠陥として直した**（[[aim-tree]]）: **宣言していない repo へ届かないこと**と、**届いた先で嘘をつかないこと**は別々の穴であり、片方を塞いでももう片方は残る。

**⚠ 供給したものが届くかは `version` 宣言が握っており、道具の側もそれを前提にしている。** `plugin.json` が `version` を宣言している以上、**上げない限り既存の利用者は古い cache を持ち続ける** ∴ bump は release の一部である（受ける側の門と併せて `README.md` の配布節が述べる）。⚠ **`claude plugin tag` は `{name}--v{version}` の git tag を作る際に、`plugin.json` と marketplace entry の version が一致していることを検証する** —— 「忘れれば黙って届かない」を機械で止める手が、既に道具の側に在るということである。⚠ **ただし現 `marketplace.json` の entry は `version` を宣言していない ∴ この検証が実際に何を見るかは未実測**であり、使えると書いてあるのではなく、**まだ試していない**と書いてある。

⚠ **2026-09-02、project スコープだけで供給した帰結が、受け取り側にも出た。** 宣言が project の `.claude/settings.json` にしか無い ∴ `installed_plugins.json` に bearing の record は 1 つも無く、**`/plugin update bearing` は仕事をしても一言も出さない** —— 実際に cache へ `0.7.0` が入ったのだが、それを確かめる手段は file の時刻だけだった。⚠ **成功と失敗が画面上で同じ見た目になる** ∴ 届いたかは cache の dir を見るしかない。⚠ **「ならば marketplace update だけで届く ＝ 開発中に打つコマンドが 1 つ減った」という読みは、時刻が否定した** —— あちらは先に `installed_plugins.json` を書いて仕事を終えており、`0.7.0` が生まれたのはその 31 秒後の `/plugin update` である。**減ってはいない。** ⚠ これは [[ambient-display]] が自分の面に課した「不在を黙って消さない」の、**道具の側での破れ**である —— 我々には直せない層に在る。⚠ **そして「受け取れた」という報告が無いことは、下段の 2 つの複製のうちどちらが走っているかが repo から見えないのと、同じ穴の別の現れである** —— あちらは走っている最中に見えず、こちらは受け取る瞬間に見えない。

⚠ **2026-09-03、その沈黙の正体が分かった —— 黙っていたのは update ではなく機構そのものである。** tracked な宣言（`extraKnownMarketplaces` ＋ `enabledPlugins`）は **enable はするが install はしない** ∴ `installed_plugins.json` に record が無い間、bearing は**どのセッションにも載っていなかった** —— skill 3 枚も hook 4 枚も、1 つとして走らない。⚠ **前段が「仕事はしているのに報告だけが無い」と読んだのは誤りで、載ることに関しては仕事もしていなかった**（cache へ `0.7.0` が入ったのは *update の*仕事であって、載ることとは別の事象である）。⚠ **∴ user スコープの record を外した瞬間から、bearing は自分の repo の中ですら動いていなかった** —— そして木には「動いてはいる」と書かれた。**悪いセンサーは無いセンサーに劣る**が、機構ではなく*木*の側で起きた形である。

**復旧は `claude plugin install bearing@trust-delta --scope project` の 1 手**で、`installed_plugins.json` へ `projectPath` つきの record が入る（tracked な `.claude/settings.json` は 1 文字も動かない）。⚠ **record が入ると `/plugin update` は喋り出す**（`bearing is already at the latest version (0.7.0)`）∴ **あの沈黙は道具の破れではなく、record の不在の症状だった** —— 前段の「我々には直せない層に在る」は取り下げる。

⚠ **2026-09-03、別マシンの 14 commit を取り込んだ機体で計り直したところ、「減ってはいない」が覆った。** record が `0.4.0` に留まった状態で `/plugin marketplace update trust-delta` を 1 回打つと、**cache の書き込み・marketplace の刻印・record の bump が 250ms 以内に順に起きた** —— cache の中身 `10:11:54.044` → `known_marketplaces.json` `.176` → `installed_plugins.json` `.228`（`0.8.0`・当時の HEAD の sha）。⚠ **cache は marketplace の刻印より 132ms *先に* 書かれている** ∴ 届けたのは marketplace update そのものであり、続けて打った `/plugin update bearing` は disk を 1 byte も動かさなかった。**∴ 上段の「減ってはいない」は取り下げる —— 減っている。ただし record が在るときに限る。**

⚠ **食い違いの理由は、この節が既に名指していたものだった** —— 09-02 の観測は **record が無い状態**で採られており、bump する対象が存在しない ∴ あのとき marketplace update には届けようが無かった。**同じ record の不在が、沈黙と「減っていない」の両方を生んでいた** ∴ 上段の取り下げは 1 つ足りていなかった。⚠ **一度取り下げた観測の上に、別の結論が乗ったまま生き残る** —— これは木の側で起きる drift の形であり、機構は捉えない（[[purpose-drift]] が見るのは file の移動であって、主張が依拠する観測ではない）。

**⚠ ただし `/plugin update <name>` については、2 つの観測が食い違ったままである。** 上段が記録したのは `bearing is already at the latest version (0.7.0)` という発話だが、本日この機体で 2 度打った slash command は**何も出さず、plugin 設定の UI を開いた**（引数無しの `/plugin` と同じ挙動）。⚠ **CLI の `claude plugin update` と slash command の `/plugin update` が同じものだと確かめていない** ∴ **どちらの観測も取り下げない** —— 別のものを見ていた可能性のほうが高く、それを潰す観測をまだ持たない。

⚠ **宣言と実体の食い違いは、`autoUpdate` にもう 1 つ在る。** 本 repo の `extraKnownMarketplaces.trust-delta` は `"autoUpdate": true` を宣言しており、`README.md` の配布表は「宣言すれば起動時に自動」と述べている。⚠ **だが `known_marketplaces.json` の `lastUpdated` は `2026-09-01T06:13:59Z` から 2 日間動かず**、人間が手で打つまで clone は `f6692ae` に留まっていた —— **その間に 14 commit が push されている**。⚠ **同じ機体・同じ file の中で `claude-plugins-official` は本日 01:01:05 に自動更新されている** ∴ 機構が走っていないのではなく、**この entry だけが引かれなかった**。⚠ **理由は特定できていない** —— project スコープの entry が対象外なのか、失敗して黙ったのかを分ける観測をまだ持たない ∴ **`README.md` のあの行は現在 未検証である。** 本 node はそれを述べるに留め、doc は書き換えない —— **何が正しいかを知らないまま instruction を書き換えれば、読み手を別の誤りへ送る。**

⚠ **2026-09-04、`autoUpdate` の 2 候補のうち片方が落ちた（実測、clone の reflog）。** 起動時、**人間もエージェントも何も打つ前に clone が 11 commit 分 fast-forward していた** —— reflog に `pull ... Fast-forward` が残り、同じ file の `claude-plugins-official` はその **1.5 秒前**に刻まれている ∴ **起動時の巡回であって、誰かが打った 1 手ではない**。宣言は前段と同じ **project スコープ ＋ `"autoUpdate": true`** である。⚠ **∴ 「project スコープの entry が対象外」は偽である。** ⚠ **だが前段の観測も同じ reflog が裏づけた** —— `09-01` の clone から `09-03` まで **pull は 1 本も無い**（その間に 14 commit が push されている）。∴ **同じ宣言が、引かれる日と引かれない日を持っている** —— 落ちたのは候補 1 つであって、問いではない。⚠ **∴ README の当該行には 2 件を両方書いた**（下の `[done]`）—— **「宣言すれば必ず」と読ませないことが、今できる最も正確な記述である。**

⚠ **同日、3 つ目の門にも実測が付いた —— install を打たずに record が生まれた。** `/plugin` の UI で更新し、セッションを立ち上げ直した直後、**tracked な宣言から project スコープの record（その時の最新版）が生えていた** —— **install コマンドは打たれていない**（人間の証言 ＋ 時刻）。⚠ **UI の更新と再起動のどちらが生んだかを分ける観測は無い。** ∴ **「宣言は enable であって install ではない」は真のままだが、「受ける側が install を 1 手打つ」は唯一の道ではない。** ⚠ **これは `README.md` が「まだ測っていない」と名指した *信頼ダイアログを受け入れる瞬間* とは別の経路である** —— あちらは依然として未測であり、ここで埋まってはいない。

⚠ **∴ 上段の二律は、より鋭い形へ言い直される。** 「tracked ゆえ載っている事実自体が git に残る」は**半分しか真でない** —— git に残るのは**載せる意志**であり、**載っている事実は untracked な `installed_plugins.json` にしか無い**。∴ **project スコープを選んでも untracked の罠は消えず、効く範囲が狭まるだけである。** ⚠ **意志と事実は黙って食い違いうる** —— 実際に丸 1 日食い違った。

⚠ **供給された機構は、この repo の中では 2 つ在る。** hook の宣言は `${CLAUDE_PLUGIN_ROOT}` に釘付けで cache を走らせ、statusline は project の path に釘付けで working tree を走らせる ∴ **同じ機構の 2 つの複製が同時に走り、片方だけが新しい**。⚠ **2026-09-02 に実際に食い違った** —— statusline は照合記録を読んで flag を落としたのに、hook は cache 0.5.0 を走らせて同じ flag を出し続けた。**この repo は自分自身の古い版を食べていた** ∴ ドッグフーディングは bump の間隔だけ遅れる。

⚠ **これを「揃える」と、開発の即時性と配布の一貫性のどちらかを失う。** ∴ 揃えるのではなく**選ばせた**: cache の複製は、自分が bearing の checkout の中に居ると分かったとき working tree 側へ委譲する。開発中は working tree、他 project では cache が*自動で*選ばれる。⚠ **判定は repo の code に載る** —— cache を working tree へ symlink する案と違い、machine-local な不可視の細工にならない。⚠ **ただし shim は cache 側に住む ∴ 版の門は消えず、「毎リリース効く門」から「shim を変えたときだけ効く門」へ縮むだけである。**

⚠ **2026-09-03、供給の層で 2 文字が欠けていたことが分かった。** carrier が名指す CLI は `$CLAUDE_PLUGIN_ROOT`（波括弧なし）で書かれており、**inline 展開されず文字列のまま skill に載っていた** —— そして **Bash tool の env にその変数は無い** ∴ 指示どおり打ったエージェントは `/bin/handoff.mjs` を見て落ちる。⚠ **4 セッション連続で起きており、その間ずっと「道具の側の癖」として baton に書き継がれていた。** docs が inline 展開を明記しているのは `${CLAUDE_PLUGIN_ROOT}` の形であり、`--plugin-dir` で working tree を載せた子セッションで**実際に絶対 path へ展開されることを確かめた。** ⚠ **hook が吐く text は展開の対象ではない**（対象は hook の `command` field である）∴ あちらは placeholder を使わず `import.meta.dirname` で解いた —— **env に一切依らず、しかも*今走っている複製*を名指す**（委譲されていれば working tree を指す）。

⚠ **同日、その同じ 1 行の下にもう 1 つ欠陥が在った —— そして見えていた破れを直しても、こちらは現れなかった。** `import.meta.dirname` で path を*解決*する形にしても、それを**シェルへ載せる形**は `JSON.stringify` のままだった。⚠ **JSON は shell quoting ではない。** POSIX では偶然ちょうど正しく、Windows では backslash が二重化されていた —— そして argv を実測すると、**2 つのシェルは別々の機構で通っていた**: Git Bash は POSIX の unescape が `\\` を `\` へ畳んで **JSON のエスケープをちょうど打ち消しており**（Windows の性質ですらない）、PowerShell は畳まず二重のまま渡し、**path 正規化が繰り返し separator を吸収していた**。⚠ **そして偶然は drive letter で終わっていた** —— 先頭の `\\` だけは UNC の印ゆえ規則が違い、`\\server\share\x` は JSON escape を経ると先頭 1 本に畳まれて UNC でなくなる ∴ **network path に載った plugin は PowerShell から解決できない。**

⚠ **これが前段と違うのは、壊れ方が声を持たなかったことである。** 2 文字の欠落は 4 セッション連続で*落ちた*ので、いずれ見つかるほかなかった。こちらは **2 日以上、一度も落ちずに走っていた** —— 動いていたのは正しかったからではなく、**2 つの偶然がたまたま同じ答えを出していたから**である。⚠ **∴「動いている」は「正しい」の証拠ではない**、という [[purpose-drift]] の法が、センサーではなく*供給*の側で成り立った形である。

⚠ **塞げるところまで塞ぎ、塞げない境界を書いた。** win32 でだけ separator を `/` へ倒す —— backslash が 0 個なら、JSON にもどのシェルにも escape する対象が残らない ∴ 偶然に頼るのをやめる。⚠ **POSIX では 1 文字も変えない**（あちらの filename には `\` が合法に現れる ∴ 無条件の置換は別の path を作る）。⚠ **そして `$` / backtick / `"` を含む path は原理的に閉じない** —— **出力先のシェルが emission の時点で分からない**以上、1 つの文字列で全シェルの quoting 規則を同時に満たすことはできない。∴ ここは「観測できていない」ではなく「**塞げないと分かっている**」であり、**その 2 つを同じ沈黙に畳まない**ために `lib/shell.mjs` がそう書いている。

⚠ **2026-09-03、license が MIT に決まった**（人間の act）。⚠ **「供給する」は、機構が動くこととは別の層でも止まりうる** —— `LICENSE` が無い間、この plugin は public repo に置かれながら**読めるが誰も合法には使えない**ものだった。⚠ **そして root に 1 枚置くだけでは届かない**: marketplace entry の source は `./carriers/claude/bearing` であり、**cache へ複製されるのはその subtree だけである**（実測: cache 直下は README 2 枚と `bin` / `commands` / `hooks` / `lib` / `skills` / `test` のみ）∴ MIT が要求する「複製に著作権表示を含める」を満たすには、**carrier 自身が 1 枚持つほかない** —— これは重複ではなく配布物の一部である。⚠ **その複製は生成物にした**: 手で置けば root が動いた日に黙って古くなり、**しかも古くなったことは配布物の側にしか現れない** —— 上段が名指してきた「意志と事実は黙って食い違いうる」の、license の層での形である。

**3 点である根拠は、今のところ「実際に足りなかった」以上のものではない。** 人間のワークフローで標準ハーネスだけでは満たされなかったものが、目的の保持・文脈の引き継ぎ・ズレの検知の 3 つだった。⚠ **これは基準から導いた選別ではなく、観測された欠落である** ∴ 4 つ目が現れれば `aim:` は動きうる。抽象的な基準を先に立てて 3 つを演繹しなかったのは意図的で、**制約は検証から後ろ向きに accrete する**という規則に従っている。

**⚠ [[native-language]] は、その「4 つ目」ではない。** あれが述べるのは*この repo がどう書かれるか*の制約であって、標準ハーネスに**観測された欠落**ではない —— 母語に揃えるかどうかは器の性質ではなく、この project が自分自身に課す規律である。∴ 上の条項を発火させず、`aim:` は動かない。⚠ **この判別はエージェントの call であり、人間が覆せる。**

**⚠ 2026-09-02、人間が「3 点は 3 点である」と述べた。** [[ambient-display]] と [[surface-parity]] が扱う statusline は、**公式 CLI という標準ハーネスが既に備える機能**であり、そこへ ctx / aim / drift を載せているのは*我々が付与する追加の情報表示*にすぎない ∴ **4 つ目の欠落ではなく、3 点をスムーズに遂行させる手段である。** `aim:` は動かない。

⚠ **この判定は「属するか属さないか」の二分からは出なかった。** エージェントは、1 行目が運ぶ context / plan usage が上の 3 語のどれにも収まらないことまでは言えた —— だが*遂行を助ける*という**第三の関係**を立てられるのは、目的を持つ側だけである。⚠ **この差は「4 つ目が現れれば `aim:` は動きうる」の運用そのものに効く**: 新しい関心が現れるたび、それが柱なのか柱を支えるものなのかを問い直すのは人間の act であり、機械が数えられるのは*どれにも一致しない*ところまでである。

**⚠ この corpus は 2026-09-01 に新規に播かれた。履歴を読む fence は当面 構造的に沈黙する。** `drift-intra` は anchor の*変更* commit を trigger にし、`drift-inter` は「一緒に commit されていない隣接」を見、`checkpoint-stale` は `last-verified` の経過を数える。誕生 commit しか無い corpus では 3 枚とも空になる。⚠ **空を「健全」と読んではならない。** これは道具が壊れているのでも corpus が綺麗なのでもなく、**噛むものがまだ無い**という第 3 の状態であり、区別できるのは本節がそう書いてあるからだけである。

**履歴を運ばなかったのは、公開の重みより先に、前提が違うからである。** 3 機構は前身プロジェクト `tmai`（*tactful multi agents interface*）の中で育ったが、あちらの履歴は一貫して「**tmai という場所の中での役割・手段**」を前提に書かれている。本 repo が問うのは「**外付け拡張としてどうか**」であり、出発点が違う。⚠ **履歴を運べば、あちらの判断を正しかろうと誤っていようと丸ごと継承する** —— これは前身が 2026-08-31 に「正本は実装ではなく `aim:` 文である」と判断したときに退けたのと、同じ構造の誤りである。∴ **概念は継承し、履歴は継承しない。** 3 柱はここから別の道を歩みうる。

**⚠ 2026-09-03、人間が決めた —— plugin 側は自分の実体が置かれているかを検査しない。** 「載っていない」を hook と skill の面に出す道は、この repo の外へ出ないと無い（**載っていなければ 1 枚も走らない** ∴ plugin の中からは原理的に述べられない）。残っていた手は user スコープへ戻るか project の `CLAUDE.md` に期待を書くかで、⚠ **どちらも採らない。通例に合わせる。** ⚠ **これは諦めではなく実測の上の選択である** —— 公式 marketplace の 54 plugin（公式 39 ＋ 外部 15）を測ると、**自分の不在を察知しようとするものは 0 件、実行時に自分の版を述べるものも 0 件**で、`plugin.json` に `version` を宣言するものすら 13/40 でしかない。「not installed」を扱う記述 8 件は**すべて外部依存**（`cloc` / `delta` / `gh` / `PyYAML` / SDK）についてであり、**自分自身の不在を扱ったものは 1 つも無い**。⚠ **この層に対してエコシステムが実際に行っているのは harness への issue である**（#15754「plugin skill は自分の path も version も知らない」・#39307・#52218・#18517・#31462）∴ **不在も版の腐りも、plugin が担う層ではなく harness の欠落として扱われている。** ⚠ **前例の不在は、費用が安いことの証拠にも高いことの証拠にもならない** —— 誰も問うていないだけであり、user スコープにも `CLAUDE.md` にも前例は無かった ∴ どちらを選んでも最初の 1 例になる。**通例へ寄せたのは、その賭けを負わない選択である。**

**⚠ statusline だけは例外として検知対象に残る —— それは statusline が plugin の範囲ではないからである。** [[ambient-display]] の装着 shim は `~/.claude/` に住み **plugin の外に在る** ∴ 載っていなくても走り、そう描く。⚠ **上の通例が述べるのは「plugin は自分の実体を検査しない」であって「不在は誰にも述べられてはならない」ではない** ∴ plugin の外に立つ面が述べることは、通例と矛盾しない。⚠ **∴ hook と skill が黙ることは、以後は未了ではなく、負うと決めた費用である。**

**⚠ 2026-09-03、供給の形を組み替えることを人間が選んだ。** 上の 3 択は「宣言をどこに置くか」を **1 つの単位**として問うていたが、⚠ **bearing は 1 つの単位ではない** —— [[session-handoff]] の 2 枚と [[ambient-display]] の 1 行目は `docs/aims/` に何も依存せず**どの project でも使える**のに対し、aim の規律は corpus を前提にし、**採っていない repo では邪魔になる**。∴ **分ける**: plugin 本体は user スコープで全 project に載せ、**aim の規律だけを project ごとの opt-in にする。** ⚠ **これは 2026-09-02 の判断の取り下げではなく、その理由 ⑵（他人の repo で明らかに邪魔になる）を譲歩ではなく分割で解いたものである** —— 邪魔になるのは bearing 全体ではなく aim の層だけだった、というのが分割の根拠であり、理由 ⑴（user スコープは「より汎用的で普遍なもの」の置き場）も、そこへ行くのが `docs/aims/` に依存しない部分だけになることで満たされる。

**⚠ この組み替えは、上段の二律が効かない場所へ問いを移す。** plugin の載り具合は **全 project で 1 つ**になり（∴ 差は machine ごとに 1 回で、[[ambient-display]] の shim が描ける）、**project ごとの差は「aim を採ったか」だけになる —— そしてそれは tracked な file に残る。** ⚠ **∴ 「全 project に効く」と「載っていることが後から辿れる」の両立不能は、消えるのではなく、噛む対象が変わって軽くなる。**

**⚠ 法と事実は別の層に置く。実測した差がそれを決める。** SessionStart hook の出力は**会話として要約され消える**（docs: 「Context that hooks added earlier — Summarized with the rest of the conversation」）が、**project-root の `CLAUDE.md` は compaction 後に disk から再注入される**。⚠ **さらに `CLAUDE.md` は subagent にも階層ごと載る**（docs 明記。組み込みの Explore と Plan だけが除かれる）のに対し、**docs が subagent の lifecycle として挙げるのは `SubagentStart` / `SubagentStop` であり、SessionStart がそこで発火するとは述べていない** ∴ **frame が subagent に届いている根拠は無い**（⚠ これは docs の読みであって実測ではない）。⚠ **どちらも「強い／弱い」の差ではない** —— docs は両方を「context であって強制される設定ではない」と述べ、**system prompt に載るのはどちらでもない**（`CLAUDE.md` は system prompt の後の user message として届く）。**差は位置と消えなさだけで、それが層を決める**: **静的な法は `CLAUDE.md`、実行時にしか出せない事実（fence・数・時刻）は hook。**

**手段は `CLAUDE.md` へ差し込む marker 付きの block である。** ⚠ **marker は HTML コメントにする** —— docs が「block-level の HTML コメントは context へ注入される**前に**除かれる」と明記しており、**Read tool で開けば人間には見える** ∴ **識別子・版・本文 sha を持たせても、消費者の context を 1 token も食わない。**（⚠ 条件が 2 つ: **独立行に置く**こと —— block-level でなければ除かれない ——、そして **fenced code block の中に置かない**こと —— code block 内のコメントは保存される ∴ 逆に context へ乗る。）⚠ **本文 sha を持つことで「人間が手を入れた」と「版が古い」を別物として述べられる** —— 持たなければ、どちらも「中身が違う」に畳まれる。⚠ **どちらの場合も置き直さず、述べて止まる**（`/bearing:statusline-setup` が既存の statusline を上書きしないのと同じ規律 —— **どれが正かを機械が決めてよい場面ではない**）。⚠ **入れる道と同時に出る道を作る** —— 他人の repo に置く機構が外し方を持たないのは不誠実である。

**⚠ そして marker は識別子であると同時に opt-in の宣言になる**（この分割そのものは [[supply-scope]] が持つ）**。** 現在 hook は「`docs/aims/` が無い」を 1 つの経路に畳んでおり、**aim と無関係な他人の repo と、aim を採ったが node がまだ 0 の project とを区別できない**。marker はその 2 つを分ける ∴ **前者では完全に黙り、後者では最初の node を作る手順を述べられる。** ⚠ **区別を持たないままでは、全 project に載せる形はそもそも採れない** —— 毎セッション「この project は aim を採っていない」と述べる機構は、**まさに 2026-09-02 に人間が外した邪魔さそのものである。**

**⚠ 更新は、同じセッションの中で 2 つの面に別々のタイミングで届く（2026-09-03、実測）。** 新しい `with-aim` コマンドが「載っているのに使えない」形で 2 度失敗し、原因は供給ではなく**順序**だった —— SessionStart hook が発火したのが `09:17:29.815Z`、cache に `commands/with-aim.md` が生えたのが `09:17:49` である ∴ **command 一覧が作られた 20 秒後に、command の実体が生まれていた。** ⚠ **同じセッションで PATH は追随していた** —— Bash tool の PATH は `0.10.0/bin` を指しており、`bearing-with-aim.mjs` は解決した。∴ **追随しないのは slash command の一覧だけであり、同じ plugin の同じ更新に対して 2 つの面が別々に固まる。**

⚠ **∴ 正しい順序は「更新してから開き直す」であって、その逆ではない。** そして ⚠ **「載っている」かどうかを見ても、この失敗は説明できない** —— record も cache も正しく `0.10.0` を指していた。**供給の正しさと、今このセッションが何を持っているかは、別の問いである。**

⚠ **同日、3 つ目の届き方が観測された（人間の観測）—— statusline だけは再起動を要さない。** `/bearing:statusline-setup` を打った瞬間、**装着より前から開いていた 3 つのセッションすべてに面が現れた** ∴ **`statusLine` の設定は live に拾われる。** 理由は 2 つあり、どちらもこの面に固有である: **描画のたびに新しい process が立つ**こと、そして**装着の shim が走るたびに install record を読む**こと。⚠ **∴ 同じ 1 つの更新に対して、この plugin は 3 つの異なる時定数を持つ**: slash command の一覧は**セッション開始時に固まり**、Bash tool の PATH は**セッション中に追随し**、statusline は**設定の変更ごと即座に切り替わる**。⚠ **「載っている」は 1 つの事実だが、「今この面が何を持っているか」は面の数だけ在る。**
⚠ **置き先は `./CLAUDE.md` に限る。** docs は `./.claude/CLAUDE.md` も project instructions と認めるが、**compaction 後の再注入の表は "Project-root CLAUDE.md" としか書いておらず、あちらが含まれるか読み取れない** ∴ **確実な方だけを使う。** ⚠ **2026-09-03 の実装では「両方在れば止まる」を採らなかった** ——止まる理由は「どれが正か機械が決められない」ときに在るが、書き先は最初から 1 つに決まっている ∴ 曖昧さは無い。**`./.claude/CLAUDE.md` は触らず、在ることだけを述べる**（人間の file を我々の都合で動かさない）。⚠ **`.claude/rules/` に 1 枚置く道も在り、`paths:` 無しなら `CLAUDE.md` と同じ優先度・同じ再注入を持つ** —— **人間が `CLAUDE.md` を選んだ**（2026-09-03）。⚠ **費用は隠さず明示する**: 差し込む法は 12 行（約 1.9KB）で、`CLAUDE.md` の 200 行目安に対して **約 6% を消費者の予算から取る。**

# PROCESS

- [done] repo を立て、名前を**守る対象**で定めた。前身は名に機構（multi agents）と形（interface）を焼き込み、1 年で両方が偽になった ∴ 「bearing」＝ 保つ方位と負う荷重、という 2 義に賭けた
- [done] plugin を移設し、`bearing` として標準ハーネス上で動かした。⚠ **これは「動いている」という実装の事実にすぎない** —— 足りているかは下の `[todo]` がなお問うている
- [done] **`_guide` を中立化し、carrier を生成し直した。** 実測すると分布は極端に偏っていた —— `frame.md` は既に完全に中立（0 箇所）、`aim-authoring.md` と `handoff.md` は各 1 箇所の書き換えで済み、`README.md` と fence 仕様は書き直し、`エージェント-posture.md` は**丸ごと削除**した（あれは engine が system-prompt に注入するための doc で、注入経路が無い以上ここでは実体を持たない）。⚠ **方法の核ほど中立だった**という事実そのものが finding である —— 場所への依存は方法ではなく*配置と姿勢*の層に溜まっていた。あわせて plugin の code / test / 生成器からも、解決しない node 名と実装比較の記述を落とした（導出の理由は残した）
- [todo] **標準ハーネスだけで 1 サイクル回し、何に詰まったかが残る形にして、人間が「足りない点は本当に 3 つか」を実測できるようにする。** ⚠ **実測そのものは人間の act である** —— エージェントがここで終えるのは、その観測が可能になるところまでで、それ以上は原理的に越権になる。⚠ テストが通ることは「壊れていない」の証拠であって「足りている」の証拠ではない ∴ **通過本数はこの観測の材料にならない**
- [done] **fence tag を `tmai-aim-*` から `bearing-*` へ改名した。** セッションが parse する契約ゆえ破壊的変更だが、外部の消費者がまだ 0 である今が最も安く、⚠ **眠らせる前身の名を、生き残る側の機械契約が名乗り続ける**状態を残す方が高くつく。5 枚の fence と `bearing-trace`、および prose 中の例示まで揃えた —— 存在しないタグを例に出す doc は、それ自体が読み手を誤らせる
- [done] **cache の複製が、bearing の checkout の中では working tree 側へ委譲するようにした。** 判定材料が 1 つでも欠ければ委譲しない（安全側は「委譲しない」）。⚠ **`plugin.json` の `name` まで見る** —— 他 project が bearing を vendor していれば同じ path は在りうる ∴ path の一致は実行の根拠として弱すぎる。⚠ stdin は fd ごと渡す（`stdio: 'inherit'`）∴ **委譲は stdin を読む前に走らねばならない**
- [done] **自分が載っているかを、面に出す。** ⚠ **2026-09-02 → 03 の丸 1 日、bearing はどのセッションにも載っていなかったのに、repo からも面からもそれが見えなかった** —— `installed_plugins.json` に record が無い状態は、statusline にも fence にも `bearing-trace` にも現れない。⚠ **載っていない機構は自分の不在を報告できない** ∴ 出せるのは載っている側だけであり、まず出せるものを出す（どの複製が走っているか・どの record 由来か）。**不在そのものの検知はこの層では閉じない** ∴ 形が決まらないなら escalate する。⚠ **2026-09-03、statusline の面だけは閉じた** —— [[ambient-display]] の装着 shim は `~/.claude/` に住み **plugin の外に在る** ∴ 載っていなくても走り、そう描く。**残るのは hook と skill の面である**: あちらは載っていなければ 1 枚も走らない ∴ **plugin の中からは原理的に述べられない**。⚠ **2026-09-03、人間が決着させた**（上の IS）—— **plugin 側は追わない** ∴ **この行は statusline の面で閉じる。** hook と skill の穴は残るが、それは残務ではなく、通例に合わせて負うと決めた費用である
- [done] **carrier が名指す CLI から、壊れた env 参照を除いた。** skill は `${CLAUDE_PLUGIN_ROOT}`（波括弧つき ＝ docs が展開を明記している形）へ、hook の出力は `import.meta.dirname` へ。⚠ **後者は placeholder ですらない** —— hook の吐く text は展開されないうえ、`import.meta.dirname` なら env の有無に依らず*今走っている複製*を名指せる
- [done] **シェルへ載せる path の形を `lib/shell.mjs` へ出し、UNC で壊れる経路を塞いだ。** win32 でだけ separator を `/` へ倒す（backslash が 0 個なら escape する対象が残らない）。⚠ **同じ行が 2 つの hook に在った** ∴ 理由ごと lib へ移した —— 上段の `${CLAUDE_PLUGIN_ROOT}` の説明を 2 箇所に複製したまま 3 つ目の懸念を足せば、正本が 3 重になる。⚠ **`platform` を引数に取る** —— `process.platform` を見るだけにすると **CI（ubuntu）から win32 の分岐を検査できず**、直したばかりの「片方の platform でしか効かない門」を同じ commit で作り直すことになる
- [todo] **hook 間の cwd 解決を一致させ、一致が保たれることを検査で固定する。** `bin/aim-facts.mjs` は `process.cwd()` を使い、`boot-ritual` / `corpus-delta` / `precompact` は `input.cwd || process.cwd()` を使う。⚠ **2026-09-01 に、これは「原理上ありうる」ではなくなった**: エージェントが `cd` すれば `input.cwd` は移動先を指し、`process.cwd()` とも食い違う。[[ambient-display]] はそこへ **3 つ目の解決**（`workspace.project_dir` を優先し、branch だけ `current_dir` から取る）を持ち込んでおり、⚠ **実際に corpus を見失って発覚した** —— composer が他と違う unit を読む形は、もう起きている
- [done] **license を MIT にし、carrier へ届く形にした。** `LICENSE` は root の 1 枚が正本で、`gen/claude-plugin.sh` が plugin へ写す（**無ければ生成を止める** —— 著作権表示を欠いた配布物を作らせない）∴ 食い違いは既存の CI の門が赤くする。`plugin.json` と marketplace entry も `license` を宣言する。⚠ **宣言が効くことは変異試験で確かめた** —— 綴りを `licence` に崩すと validator が「Unknown field 'licence' — did you mean 'license'?」と述べる ∴ **通ったことは検証器が field を知っている証拠である。何でも通す validator の緑は証拠にならない。⚠ **届いたことも実測した**（2026-09-03、merge 後に `/plugin marketplace update trust-delta` を 1 手）: cache の `0.9.0` 直下に `LICENSE` が在り、**`0.8.0` には無い** ∴ 上段の「root の 1 枚は届かない」は予測でなく観測になり、生成された複製が実際にその穴を埋めている。⚠ **同じ cache の複製が `escalation: 1` を描く** —— 他 project が受け取る版で、供給の両端が繋がった。⚠ **そして record が在る今、marketplace update は喋る**（`Updated 1 marketplace (1 plugin bumped)`）∴ 上段の「沈黙は record の不在の症状だった」は、update の側からも確かめられた**
- [done] **carrier の frontmatter が黙って捨てられる形を、機構で止めた。** ⚠ **2026-09-03、実際に壊した** —— `argument-hint: [--dir <path>] [--check | --remove]` は YAML の flow sequence が 2 つ並んだ形で parse に失敗し、⚠ **例外ではなく「全 field を黙って捨てる」に落ちる**（`claude plugin validate` の言葉で「At runtime this command loads with empty metadata」）∴ `allowed-tools` も `disable-model-invocation` も消える。⚠ **validator は在ったが CI に無い** —— **手で打つ門は、忙しいときにだけ飛ばされる**。⚠ **YAML は実装しない**（依存を足せば「build 手順も無い」を破る）∴ **観測された壊れ方 1 つに絞った規則**を置いた: `[` で始まる値は quote する。⚠ **既存の `statusline-setup` も同じ形だった** —— あちらは parse に失敗しないが、**文字列ではなく配列として渡っていた**
- [done] **`README.md` / `README.en.md` の配布表に、`autoUpdate` と install の 2 行について実測を書いた。** ⚠ **書き換えたのは*振る舞いの記述*ではなく*確からしさの記述*である。** `autoUpdate` の行には**食い違う実測 2 件**を並べた（`09-01`→`09-03` の 2 日間 pull 無し ／ `09-04` 起動時の 11 commit の自動 pull。**どちらも同じ project スコープの宣言**）∴ 読み手は「宣言すれば必ず引かれる」とは読めない。install の行には、**install を打たずに record が生まれた経路**（`/plugin` の UI 更新 → 再起動。2 つを分ける観測は無い）を足し、**あの 1 手は確実な道であって唯一の道ではない**と述べた。⚠ **どちらも上段が禁じた「知らないまま instruction を書き換える」には当たらない** —— 述べたのは観測であって、振る舞いの断定ではない
- [done] **`/bearing:with-aim` を実装し、`CLAUDE.md` の marker 付き block を置ける／置き直せる／外せるようにする。** ⚠ **人間が手を入れた block と、版が古い block を別物として述べ、どちらの場合も置き直さず止まる** —— 判定材料は marker が持つ版と本文 sha である。⚠ marker が 2 組以上／片方だけのときも同じく止まる。⚠ **実装して 2 つの不具合が出た。** ⑴ **置いて外すたびに、他人の file の末尾が 1 行ずつ伸びていた** —— `split(改行)` は末尾改行を最後の空要素として既に運んでいるのに、書き戻しが改めて足していた。**round-trip の試験（置く → 外す → 原文と一致するか）だけが捕まえた** ∴ 置く側だけを見る試験では出ない。⑵ 差し込んだ 1 行で、全角の閉じ括弧の直後に半角空白が残っていた —— **他人の doc の中で、我々の 1 行だけが組版を外す**
- [done] **hook が marker を読み、opt-in していない repo では完全に黙るようにする。** ⚠ **現在は corpus の不在を 1 つの経路に畳んでいる** ∴ 他人の repo と「採ったが node が 0」が同じ顔で出る。⚠ 黙る形の前例は `boot-ritual` に在る（baton が無ければ発火しない）。⚠ **黙るとは出力が短いことではない** ∴ 裸の改行 1 つも書かない（実測 0 byte）。⚠ **corpus が在れば印の有無に関わらず喋る** —— 印は後から入った機構であり、**既に node を書いている repo を「印が無い」で黙らせれば、更新した日に法を失う。** ⚠ **失敗経路も同じ gate を見る** —— composer が落ちた場面で frame だけを吐けば、黙ると決めた repo に最も説明のつかない形で 2KB が現れる。⚠ **baton はこの沈黙の外に置いた**（handoff は aim ではない）。⚠ **gate が dead code を 1 つ暴いた** —— 「まだ採っていない」を述べる分岐は到達不能になっており、**到達しない分岐は扱えているという嘘である** ∴ 落とした。⚠ **既知の限界（2026-09-03、記録）: `readOptIn` は unit root の `CLAUDE.md` しか見ない** ∴ **multi-repo の unit で、member repo ごとに opt-in する道は無い。** ⚠ **2026-09-03、[[aim-tree]] の在り処の解決がこれを副産物として解いた** —— 在り処を repo ごとに読むなら、印も repo ごとに読むことになる ∴ **repo 自身の `CLAUDE.md` が先、unit root はその既定**、という順序になった。 単一 repo では表に出ないが、⚠ **[[aim-tree]] が corpus の在り処を project ごとに変えられるようにするなら、先にここが決まらねばならない** —— *印をどこで読むか*が、*repo ごとに別の dir を持てるか*を決めるからである
- [done] **置かれた block が古いことを面に出す。** ⚠ **[[ambient-display]] の shim と同じ構造の複製である** ∴ **版の門は 1 つではなく 2 つになる** —— marker が版と本文 sha を持つのは、この 2 つ目の門のためでもある。⚠ **4 つの状態を畳まずに分けた**: 無い / 今の版 / 古い / **人間が手を入れた** / 読めない。⚠ **sha は改行を正規化してから採る** —— さもなくば CRLF と LF の checkout で同じ block が別の sha を持ち、**git が改行を変換しただけで「人間が手を入れた」と報告する**
